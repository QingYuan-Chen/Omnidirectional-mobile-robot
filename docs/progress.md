# 项目进度记录

## 2026-07-11：启动规划
- 用户确定三阶段实施计划。
- 创建 `task_plan.md` 和 `findings.md`。
- 完成基线代码审查。

## 2026-07-11：M0 仓库基线
- 在 `main` 分支初始化仓库，并为 `QingYuan-Chen/Omnidirectional-mobile-robot` 配置远端 `origin`。
- 添加 `.gitignore`，排除构建输出、IDE 状态、CubeMX 临时文件、本地参考资料和厂家源码副本。
- 创建并推送基线提交 `51c2ed5`（`chore: establish STM32 firmware baseline`）。
- 将任务计划重新划分为基线、待板测和待实现状态，并把已批准的执行指令整合为 v2。

## 2026-07-11：M0 UART 接收恢复
- 修改回调流程前先验证 STM32F4 HAL 中断顺序：发生错误的接收完成回调之后，HAL 通常还会调用 `HAL_UART_ErrorCallback`；BSP 不再把这一隐式顺序作为唯一恢复路径。
- 发生错误的接收完成回调现在会设置待恢复标志。错误回调负责记录和清除错误，并仅在 `RxState` 为 Ready 时尝试一次重启；`BspUart_Service()` 提供任务上下文兜底恢复。
- 增加 UART 恢复尝试和成功计数，同时保留重启失败及硬件错误计数。
- CubeMX 命令行重新生成成功，没有覆盖 BSP 修改；CubeMX 仅输出了无关的本地第三方软件包警告。
- `cmake --build --preset Debug --clean-first`：57/57 通过；RAM 40,768 B，Flash 65,776 B。
- `cmake --build --preset Release --clean-first`：57/57 通过；RAM 40,760 B，Flash 39,276 B。
- `bsp_uart.c` 通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 语法检查和 GCC `-fanalyzer` 静态分析。
- 尚未执行硬件故障注入；UART 奇偶校验、帧、噪声和溢出错误恢复仍为待板测项。

## 2026-07-11：M1 IMU 数据质量输出
- 增加相互独立的一阶滤波加速度和角速度字段，供控制与遥测使用；ESKF 继续使用经过有效性门控但未经普通低通的 SI 样本。
- 增加由生产者维护的 `sample_age_ms`、可配置向量差突变拒绝和突变诊断计数。
- 使用真实饱和丢样总数替代 `UINT32_MAX` 时间戳跳变哨兵；单次时间戳跳变只会使受影响样本失效。
- 初始滤波截止频率和突变阈值仍需板上调试；M1.3 硬件验收延期至 M2 单电机调试阶段。
- Debug 和 Release 构建通过。RAM：40,864/40,856 B；Flash：66,660/39,696 B。
- `app_imu.c` 通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 语法检查和 GCC `-fanalyzer` 静态分析。

## 2026-07-11：M1 IMU 非阻塞重试退避
- 将 IMU 任务的固定阻塞延时替换为应用层维护的 `20/50/100/200/500 ms` 重试序列，最大 500 ms；首次成功读取后复位。
- 退避期间，`AppImu_Process()` 跳过 I2C 访问，但 IMU 任务仍按正常有限周期唤醒，发布当前数据年龄和陈旧状态，并在每次执行时上报心跳。
- 在 `AppImuOutput` 中增加可观测的退避次数、当前延时和下一次重试时刻字段。
- Debug 和 Release 构建通过。RAM：40,888 B；Flash：66,900/39,764 B。
- `app_imu.c` 和 `app_tasks.c` 通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 语法检查和 GCC `-fanalyzer` 静态分析。

## 2026-07-11：M1 IMU 故障分级与稳定恢复
- 增加健康、瞬时降级、持久传感器故障、恢复中和估计器故障状态；关键任务心跳状态与 IMU 健康状态分开发布。
- 单次 I2C 错误、时间戳不连续或突变拒绝只会使受影响输出失效；连续三次传感器或突变失败才升级为持久传感器故障。
- ESKF 故障单独分级，并触发使用静止标定零偏重新初始化。只有连续八个时间戳连续的样本通过，且估计器已初始化并保持有限，才恢复 `DATA_VALID`。
- 安全逻辑现在使用可恢复的 IMU 健康状态禁止运动，不永久锁存机器人；重复的关键任务心跳丢失仍会锁存。
- M1.3 板上验收延期至 M2 单电机调试。当时曾把 M2 记录为速度 PI + 前馈且不使用伪电流环；控制器和频率选择已在 2026-07-14 的 M2 决策门中重新开放并取代该旧结论。
- Debug 和 Release 完整构建 57/57 通过，最终状态严重度修正后的增量构建也通过。RAM：40,928 B；Flash：67,552/40,116 B。
- `app_imu.c`、`app_tasks.c` 和 `imu_eskf.c` 通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 语法检查和 GCC `-fanalyzer` 静态分析。

## 2026-07-14：重新开放 M2 控制架构决策
- 将参考固件的 100 Hz 控制任务和当前 20 kHz PWM 重新定义为实现基线，而非最终设计结论。SYSCLK 保持 168 MHz，PWM、可选电流环、速度环和底盘环分别管理。
- 增加 `m2_control_architecture_gate.md`，以 G0-G7 覆盖电机/硬件确认、TIM6/TIM7 确定性时基、开环辨识、M/T 测速、电流反馈硬件评审、统一候选控制器、板上对比和最终架构决策记录。
- 当时把 PI + 前馈改为基准控制器，把 LADRC 和 PI + DOB 作为同级候选；该阶段性定位已被 2026-07-15 的 G1.0 资源审查取代，当前以 LADRC 为基准。
- 当前 H60 控制板具备 AT8236 硬件电流斩波，但没有 MCU 可见的电机电流反馈。继续禁止伪电流环；增加真实电流反馈必须经过明确的硬件决策。
- 本步骤只修改文档，没有改变 `.ioc` 或固件行为。现有 10 ms `controlTask` 仍是明确标注的占位实现，直到 G0 和 G1 试验台设计评审完成。

## 2026-07-15：确认 M2 G0 电机身份

- 确认已购电机为塔克 MC520P30_12V，12 V、1:30、1024 PPR 磁编码器版本，并记录原厂转速、电流、转矩、功率、电阻和电感参数。
- 发现目录堵转电流 3.2 A 高于控制板约 2.2 A 的 AT8236 硬件斩波阈值；斩波介入区域仍为必测项。
- 发现编码器定义存在四倍冲突：原厂 1:30 四倍频示例为每轮 122880 计数，当前固件配置为 30720。在输出轴整圈计数实测关闭该问题前，不修改固件常量。
- G0 仍需确认编码器计数/方向、允许温度、电池/机械配置和 AT8236 衰减、Brake、Coast 及斩波特性。本步骤没有改变固件或 `.ioc` 行为。

## 2026-07-15：统一项目文档语言

- 将 `findings.md` 和 `progress.md` 中原有的英文标题、说明、表头及状态语义改为中文，保留代码标识、型号、工具参数和必要技术缩写。
- 在 `plan_review_and_directives.md` 中增加文档语言规范；后续规划、流程、决策、进度和结论文档统一使用中文。
- 本步骤只修改文档，不改变固件或 `.ioc` 行为。

## 2026-07-15：确认 M2 G0 电池与机械参数

- 根据用户提供的规格图，确认电池为 LB350E 3S 21700 5000 mAh 电池组，标称/满电电压为 11.1/12.6 V，最大持续/瞬时放电电流为 8/16 A。
- 确认整车空载质量 2.2 kg、最大额外载荷约 10 kg、轮径 77 mm，电机安装和出线方式与参考底盘及源码约定一致，主要机械结构为金属材质。
- 识别到电源能力边界：四电机目录堵转电流合计 12.8 A；按 AT8236 约 2.2 A/路斩波估算的四路电流仍约 8.8 A，略高于电池持续放电能力，必须进行带载压降、BMS 保护和同时启动实测。
- G0 仍需关闭 MA 编码器整圈计数、四轮承重、BMS 动作值、量化温度上限及 AT8236 衰减、Brake、Coast 和斩波边界。本步骤没有改变固件或 `.ioc` 行为。

## 2026-07-15：简化 M2 电机方向确认

- 电机接线、安装和出线方式与参考项目一致，M2 不再安排独立的正反转方向试验；先沿用参考项目的电机和编码器符号映射。
- MA 首次低功率开环斜坡时顺带检查指令方向与编码器速度符号。若不一致，只在 BSP/配置层修正，禁止通过 PI、LADRC 或 DOB 参数补偿方向错误。
- 其余三台电机的方向和轮序确认延期到 M4 四电机联调。本步骤没有改变固件或 `.ioc` 行为。

## 2026-07-15：完成 M2 G1.0 控制资源审查

- 确认 TIM6 已由 HAL 作为 1 kHz 时间基准使用，不能复用为控制节拍；选择当前空闲的 TIM7 作为 1 kHz 候选控制时基，参数为 84 MHz 输入、`PSC = 83`、`ARR = 999` 和数值中断优先级 5。
- 明确 CubeMX 规范输入、再生成边界、计数型任务通知、DWT 时间测量、编码器 ISR 快照和 `int64_t` 累计策略。
- 将控制器定位更新为 LADRC 基准、PI + 前馈和 PI + DOB 对比候选；基准不等于最终方案，仍需 G6/G7 统一评测和决策。
- 确认现有 `BspMotor_EmergencyCoastAll()` 会关闭 PWM 定时器和输出通道。G1.2 必须把可恢复运动禁止与破坏性急停分开；硬急停后只允许系统复位恢复，不提供串口远程重新 ARM。
- 冻结 G1.2 的 MA 单电机、840 硬限幅、1 count/ms 斜坡和 500 ms 超时边界，以及 G1.3 的 230400 baud、64 字节命令行、20 ms 遥测、非阻塞 TX 和 ADC 单一所有权边界。
- 本步骤只修改中文文档，没有改变固件或 `.ioc` 行为；板上时基、MA 运动和延期的 M1.3 验收仍待集中板测。

## 2026-07-15：完成 M2 G1.1 确定性控制节拍软件

- 保持 168 MHz SYSCLK 和 TIM6 HAL 1 kHz 时间基准不变，通过 CubeMX 启用 TIM7：APB1 定时器时钟 84 MHz、`PSC = 83`、`ARR = 999`、更新频率 1 kHz、中断数值优先级 5。
- `controlTask` 不再用 `vTaskDelayUntil()` 作为控制基准，改为等待 TIM7 的计数型直接任务通知；控制心跳由 1 kHz 节拍推导后仍按 10 ms 发布。
- TIM7 向量入口采集 DWT 时间戳，HAL 回调只计算周期诊断、读取四路编码器原始计数、发布带序号的 16 槽快照并通知控制任务；控制计算、UART、ADC 和字符串格式化均未进入 ISR。
- 使用序号环保证“已取通知”与对应编码器/时间快照一致。若目标槽已被覆盖则安全失败，不会重复处理最新快照或静默使用撕裂数据。
- 将 TIM7 更新标志折叠造成的硬件漏周期、控制任务漏迭代和通知合并分别计数。硬件漏周期按启动相位累计理论应到更新数与实际 ISR 服务数之差检测，覆盖“相邻 IRQ 小于两个周期但中间仍发生折叠”的反例。
- 四路 `uint16_t` 编码器快照在任务中按模 2^16 计算 `int16_t` 增量，并用饱和 `int64_t` 累计；新路径不调用带独立静态历史的 `BspEncoder_ReadDelta()`。
- 增加实际周期、IRQ 抖动、唤醒延迟当前值/最大值/P99、完整控制循环 WCET 当前值/最大值、截止期违约和漏周期诊断。P99 微秒桶向上取整，遥测发布上一个已经完整结束的周期，避免混合不同周期的 WCET 与唤醒数据。
- 增加运行时配置一致性检查：CPU/HCLK 168 MHz、APB1 派生定时器时钟 84 MHz、TIM7 实例、PSC、ARR、IRQ 优先级和配置计算所得 1 kHz 频率任一不符均拒绝启动。
- CubeMX 命令行二次生成后，TIM7 向量入口和 HAL 回调 USER CODE 均保留；生成差异仍集中于 TIM7 配置。
- 主机测试在仓库外临时构建目录执行，编码器回绕/饱和、DWT 与序号回绕、P99 边界、通知—快照竞态、环覆盖拒绝和累计漏周期反例共 3/3 通过；临时构建目录已清理。
- Debug/Release 全量构建均为 61/61 通过。最终记录为 RAM 41,552 B；Flash 72,316 B / 42,636 B。7 个受影响源文件通过 `-Wextra -Wshadow -Wconversion -Werror` 和 GCC `-fanalyzer`。
- 两名独立审批者分别完成代码/交付质量和原框架适配/参数遗漏复审，阻断项整改后均明确通过。
- 尚未执行板上 P99、最大抖动、WCET、零丢周期、编码器实数和控制任务栈水位验收；G1.1 状态为“软件完成、待板测”，没有接入电机输出、控制器或 G1.2/G1.3 行为。

## 2026-07-15：完成 M2 G1.2 MA 安全开环链路软件

- 新增与 HAL 无关的 MA 开环状态机：上电保持未使能，显式 ARM 后才接受 PWM；硬限幅为 840，斜坡为 1 count/ms，反转先降到零并至少 Brake 1 个控制节拍。
- `STOP` 使用同一斜坡受控减速，停稳后 Brake；500 ms 命令超时强制减速到零并进入 `DISARMED`。超时减速期间新序号 PWM 和 `STOP` 都不能取消失效保护，必须停稳后显式重新 ARM。
- 只允许 MA 进入普通 Drive/Brake；每个正常输出周期都把 MB、MC、MD 强制为 Coast。G1.2 提交完成时没有 UART 运动命令调用者，因此该阶段固件不能自主驱动电机；G1.3 后续已接入受安全协议约束的调用者。
- 新增独立安全策略：首次关键心跳窗口缺失或 IMU 非健康只触发普通 Coast 型运动禁止；连续 3 个关键心跳窗口缺失或外部硬故障才锁存并调用破坏性的 `BspMotor_EmergencyCoastAll()`，只能系统复位恢复。
- Control 与 Safety 使用不屏蔽 TIM7 ISR 的短任务级执行器仲裁区，完整串行化“最终安全门复核—PWM 提交”和“安全状态发布—Coast/Emergency”，关闭普通 Coast 被旧控制快照覆盖的竞态；仲裁开销计入控制周期 WCET。
- 关键心跳改为单次 `osEventFlagsClear()` 并使用其清除前位值，消除 `Get` 与 `Clear` 之间到达的新心跳被误清除的窗口。IMU 任务在退避期间继续按原有路径上报心跳。
- 主机测试在仓库外临时目录执行，原有 3 组和新增开环状态机/安全策略共 5/5 通过；覆盖限幅、斜坡、反转、STOP、超时、tick/序号回绕、ESTOP 锁存、可恢复禁止、关键故障升级、计数饱和和超时不可绕过。
- Debug/Release 全量构建均为 63/63 通过；最后一处超时 STOP 门槛修正后对应源文件已重新编译并重新链接。最终记录为 RAM 41,584 B；Flash 75,100 B / 43,788 B。3 个新增或主要受影响源文件通过 `-Wextra -Wshadow -Wconversion -Werror` 和 GCC `-fanalyzer`，严格告警与静态分析为 3/3 通过。
- 代码/交付质量与原框架适配/参数遗漏两名独立审批者在阻断问题整改后均明确通过。
- 尚未执行板上 PWM 波形、MA 低功率运动、普通 Coast/Brake、故障注入、WCET、抖动、栈水位和延期的 M1.3 联合验收；G1.2 状态为“软件完成、待板测”。

## 2026-07-15：完成 M2 G1.3 UART4 命令与遥测软件

- 实现大写 ASCII 临时协议：`ARM <seq>`、`PWM <seq> <value>`、`STOP <seq>`、`ESTOP`、`STATUS`。LF 前最多 64 B，并允许尾随 CR；严格检查 uint32/int32 溢出、±840 边界、控制字符、旧序号和行溢出。
- `ARM/PWM/STOP` 共用 32 位半区间单调序号，并改为“深度 8 的有序命令队列成功入队后才提交序号”。队列满允许同序号重试；电机状态机对三类命令统一消费序号，关闭跨 STOP 和队满大跳导致协议层/电机层序号漂移的问题。`controlTask` 每个 TIM7 节拍最多消费 1 条。
- 格式错误、旧序号、越界和模拟队列满测试均确认不刷新 500 ms 电机超时。`ESTOP` 绕过普通命令队列，在既有任务级执行器仲裁内立即锁存故障并调用 Emergency Coast；遥测明确报告需要系统复位。
- 删除应用层阻塞 UART 发送入口，使用 `HAL_UART_Transmit_IT()`、每端口深度 4/单帧 384 B 的帧队列和回调串链。RX/TX 任务—ISR共享索引、统计和发送所有权改为 C11 原子；发送启动先取得原子所有权再读取队首，避免完成 ISR 插入后重复发送旧帧。
- `commTask` 从 5 ms/1536 B 调整为 2 ms/2048 B。ADC 单一所有权从 `monitorTask` 移到 `commTask`，只按 20 ms 周期采样，`HAL_ADC_PollForConversion` 参数由 10 ms 收紧为 1 ms；`STATUS/ESTOP` 复用最近电池快照，不增加 ADC 频率。
- 20 ms 有界整数遥测覆盖控制时间戳/节拍、四路编码器原始/增量/累计计数、MA 目标/实际 PWM、电池、安全/复位、IMU `sample_age_ms`/健康等级、IRQ 抖动、唤醒延迟、最大 WCET、漏周期/截止期和通信故障计数。全极值帧在 384 B 内至少保留 6 B，并通过 230400 8N1/20 ms 持续吞吐静态预算检查。
- 主机测试扩展为 9/9：新增实际 `bsp_uart.c` 配合假 HAL 的回调链、HAL_BUSY、起发失败、队满、丢完成回调恢复、伪回调和起发函数返回前完成测试；同时覆盖两阶段序号提交、跨 STOP 半区间、64/65 B 行边界、空白/控制字符、PWM 边界、非法命令/模拟队满不续命及遥测全部标签/帧预算。
- Debug/Release 全量构建均为 66/66 通过。最终记录为 RAM 46,696/46,688 B；Flash 83,608/47,564 B。`app_comm_protocol.c`、`app_motor_open_loop.c`、`app_telemetry.c`、`app_tasks.c`、`bsp_adc.c`、`bsp_uart.c`、`bsp_uart_tx_queue.c` 共 7 个关键源文件通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 和 GCC `-fanalyzer`。
- 代码/交付质量与原框架适配/参数遗漏两名独立审批者在阻断问题整改后均明确通过。
- 尚未执行 UART4 板上持续收发/拥塞/错误恢复、ADC 电压标定、MA 实际运动、WCET/抖动/栈水位和延期的 M1.3 联合验收；G1.3 状态为“软件完成、待板测”。

## 2026-07-15：统一已完成自研代码的中文注释

- 注释整理范围限定在自研 `App/`、BSP 接口和主机测试，不改写 CubeMX 自动生成代码、HAL、FreeRTOS 等第三方源码。
- 删除头文件保护宏后的英文式尾注，将现有英文说明改为中文；重点为协议两阶段序号提交、TIM7 时基、IMU 非阻塞退避与稳定恢复、ESKF 输入边界、电机安全状态机、UART 原子发送所有权和任务级执行器仲裁补充中文注释。
- 注释语言扫描确认上述自研范围内每个注释块均包含中文；本次没有修改功能逻辑、任务参数、控制参数、接口或 `.ioc`。
- 主机测试全量重建后 9/9 通过；Debug/Release 全量构建均为 66/66，资源占用保持 RAM 46,696/46,688 B、Flash 83,608/47,564 B。

## 2026-07-15：补全整个 App 层中文注释

- 对 `App/` 下 42 个自研 `.c/.h` 文件完成全覆盖：每个文件均说明模块职责，公共头文件补充类型、接口契约和安全边界，源文件补充状态机、并发、时基、硬件语义和故障恢复等关键不变量。
- 配置项按通信、机械参数、控制时基、IMU、任务周期、电机安全和电池 ADC 分组说明；BSP 重点说明树莓派串口角色、编码器接口边界、IMU 配置顺序、互补 PWM、制动/空转差异和紧急停机行为。
- 自动检查确认 `App/` 每个源码/头文件至少包含一处中文注释、每个注释块均包含中文，且相对上一提交的 `App/` 差异只包含注释，没有修改可执行语句、接口、参数或 `.ioc`。
- 主机测试重新构建后 9/9 通过；Debug/Release 固件均完成全量重建，资源占用继续保持 RAM 46,696/46,688 B、Flash 83,608/47,564 B。

## 2026-07-15：按实现契约重写 App 层详细中文注释

- 对 `App/` 下 42 个自研 `.c/.h` 再次逐文件阅读实现并重写注释。公共头文件现已说明调用者、参数单位、返回语义、数据所有权、阻塞边界和安全约束；源文件重点解释时间戳/回绕、状态转换、原子内存序、任务仲裁、故障升级与恢复依据，不再停留于文件级概述。
- 注释明确区分候选参数与已验收事实：1 kHz 控制节拍、IMU 20 Hz 输出低通、突变/升级/恢复门槛、编码器倍率、有效轮径、轮距和轴距均保留后续板测或实测边界；M1.3 仍延期到 M2 单电机阶段。
- 统一系统层级描述：STM32 是下位机，树莓派是直接上位机，电脑是经树莓派连接的再上位机；UART4 当前协议只用于临时试验，USART2 正式 X-Protocol/ROS2 联调仍属于 M5。
- 两名独立审查者分别检查注释与真实代码、原框架和项目规划的适配。已修正 IMU 型号、I2C 有界同步读取、UART 中断恢复、ESKF 观测雅可比符号、执行器恢复层级、安全字段多写入者等过度承诺或错误表述；代码注释质量与架构适配复审均已通过。
- 审查同时发现既有安全缺口：持续无新 IMU 样本会清除 `DATA_VALID` 并置 `DATA_STALE`，但安全任务当前只判断 `health == HEALTHY`，尚未把陈旧数据可靠转换为运动禁止。本次严格保持纯注释范围，未夹带修改控制逻辑；问题已记入 `findings.md`，应作为下一项高优先级修复。
- 自动检查确认全部 App 注释块均含中文、无超过 120 字符的注释续行，且相对上一提交的 App 差异只有注释。主机测试全量重建 9/9 通过；Debug/Release 全量构建均为 66/66，资源占用仍为 RAM 46,696/46,688 B、Flash 83,608/47,564 B。

## 2026-07-18：固化可重复主机测试入口

- 新增 `tools/run_host_tests.ps1`，从系统命令和固定工具安装目录中发现 CMake、Ninja 与本机 GCC，不依赖终端预先配置 PATH，也不把用户名目录写入工程配置。
- 脚本使用仓库内 `build/HostTests` 作为可清理构建目录；递归清理前会验证目标仍位于仓库 `build` 下，避免错误删除其他路径。
- 配置、构建和 `ctest --no-tests=error --output-on-failure` 均显式检查外部程序退出码；任一步失败都会终止并返回失败，不再出现“配置失败但最后命令成功”掩盖整体失败的情况。
- 使用 MinGW GCC 15.2.0 和 Ninja 全量重建现有主机测试，9/9 通过；该入口将作为后续 IMU、安全门和启动就绪修复的统一回归命令。

## 2026-07-18：修复 IMU 陈旧边界与陈旧后恢复基础

- `sample_age_ms >= 20 ms` 统一判为陈旧，不再保留恰好 20 ms 的边界空窗；无符号毫秒差允许 HAL 时基自然回绕。
- 陈旧首次出现会清除 `DATA_VALID`、置位 `DATA_STALE/SENSOR_DEGRADED`、进入瞬时降级并要求重新累计 8 个稳定样本；持久传感器故障和估计器故障不会被陈旧事件降级覆盖。
- 陈旧后的第一帧只进入 `RECOVERING`，不能立即恢复有效；恢复期间遇到重复时间戳会清零连续稳定计数。普通 `BSP_BUSY` 不启动总线退避，退避轮询仍刷新年龄快照。
- 新增带当前调用时刻的 `AppImu_IsMotionUsable()` 单一运动可用判据，直接用 `last_good_tick_ms` 核验实时年龄，避免控制任务仅依赖较旧的 `sample_age_ms` 快照。判据要求 ESKF 收敛，但正常健康运行不要求每帧加速度观测都被采用，也不因单帧高振动直接禁止运动。
- 新增直接编译真实 `app_imu.c + imu_eskf.c` 的主机测试，覆盖 19/20 ms 边界、毫秒回绕、持续 BUSY、非阻塞退避、重复时间戳打断恢复、持久故障分级保持、8 帧恢复和实时运动可用判据；主机测试增至 10 项。
- 主机测试 10/10、Debug/Release 固件全量构建 66/66 通过，`app_imu.c` 与 `imu_eskf.c` 通过严格告警及 GCC `-fanalyzer`。RAM 保持 46,696/46,688 B；Flash 为 83,796/47,644 B，相对上一提交增加 188/80 B。

## 2026-07-18：修复 IMU 突变基线、ESKF 时间步与严格恢复资格

- 突变检测改为始终与“最后一次通过全部质量门的样本”比较；被拒绝的尖峰不再污染下一帧比较基线，因此“正常→单点尖峰→正常”只拒绝异常帧一次。
- 原始帧时间戳连续性与等待送入 ESKF 的时间步分开管理。尖峰帧虽然不进入估计器，但其时间步会累计到下一次成功更新；正常单点尖峰后的下一帧按两个实际采样周期推进。异常长间隔继续受 50 ms 应用层上限约束。
- 故障恢复的每一帧现在必须同时满足：时间戳步长为 1、突变检查通过、ESKF 更新成功且初始化/全状态有限、加速度观测实际被采用、未判定为高振动。任一条件失败都会把连续稳定计数清零。
- 严格条件只作用于 `recovery_required` 阶段。健康运行中的单次加速度观测拒绝或高振动仍可完成陀螺预测、保持健康和数据有效，不会把正常加减速直接误判为停车故障。
- `test_app_imu` 新增单尖峰恢复、两周期航向积分、100 步跳变后的 50 ms 上限、健康态观测门控和严格恢复计数测试；新增独立 `test_imu_eskf`，覆盖 99/100 次收敛边界、方向创新拒绝、高振动标志、非法时间步和非有限状态。主机测试增至 11 项。
- 主机测试 11/11、Debug/Release 固件全量构建 66/66 通过，`app_imu.c` 与 `imu_eskf.c` 再次通过严格告警及 GCC `-fanalyzer`。RAM 为 46,696/46,696 B，相对上一提交变化 0/+8 B；Flash 为 83,888/47,716 B，相对上一提交增加 92/72 B。

## 2026-07-18：关闭运行就绪、命令准入与执行器最终安全门

- 新增无 HAL、无 RTOS 的 `AppMotionGate` 纯状态模块。运动许可同时要求 `runtime_ready`、未被动态禁止、没有锁存故障且实时 IMU 运动判据通过；许可下降沿递增代际，使故障前已排队命令在恢复后仍保持失效。
- `runtime_ready` 改为任务层统一单向锁：只有最近安全检查窗确认 Control、Comm、IMU 三个关键任务心跳完整，且 `AppImu_IsMotionUsable()` 确认 ESKF 已收敛、样本有效健康且实时年龄未陈旧，才允许锁存。
- `ARM/PWM/STOP` 的实时许可检查、带代际请求入队和协议序号提交在同一调度仲裁区完成。许可关闭时不入队、不提交序号，恢复后可用同一序号重试；队满仍沿用相同语义。`STATUS` 和 `ESTOP` 始终独立分流。
- 控制任务在实际提交 PWM 前再次核验运行就绪、动态禁止、锁存故障、实时 IMU 年龄和请求代际。即使普通命令已经进入队列，只要执行前许可曾关闭，该请求也只计入失效诊断，不会在恢复后补执行。
- 安全策略上电即保持运动禁止，启动未就绪不会误累计关键心跳缺失；IMU 任务每次发布快照后立即刷新运动许可，且在 `BSP_BUSY`、传感器退避或数据故障期间仍继续上报任务心跳。
- 遥测增加 `runtime_ready`、许可前置拒绝数和旧队列命令失效数。为容纳全极值文本帧，UART TX 单帧上限从 384 B 调整为 416 B；主机边界测试确认仍至少保留 6 B，并低于 230400 8N1 每 20 ms 的持续字节预算。
- 主机测试 12/12 通过；Debug/Release 固件全量构建均为 67/67，RAM 均为 47,088 B，Flash 为 85,348/48,416 B。相对上一提交，静态 RAM 增加 392/392 B，Flash 增加 1,460/700 B；带许可代际的深度 8 命令队列元素由 12 B 增至 16 B，另从既有 FreeRTOS 固定堆中多使用 32 B。6 个受影响源文件通过 `-Wall -Wextra -Wshadow -Wconversion -Werror` 和 GCC `-fanalyzer`。
- 原工程架构适配/参数遗漏与工作完成质量/测试充分性两路独立审批均给出 `APPROVE`，无软件阻断项。审查确认当前 CMSIS/FreeRTOS 实现允许在调度器挂起区执行零等待队列写入；真实任务时序、树莓派遥测解析同步和板上项目仍保留为后续验收。
- 仍未执行 M1.3、UART4 实机收发、真实 IMU 故障注入、MA 低功率运动、WCET、抖动和任务栈水位板测；这些项目继续延期到 M2 单电机调试阶段，不把软件回归结果写成板上验收。

## 2026-07-18：完成 M2 板测提交一——可配置调试串口路由

- 新增 `app_debug_uart_config.h` 作为调试命令、遥测和端口统计的唯一选口位置；M2 板测镜像选择 `BSP_UART_ROS`，对应板载 Type-C 的 USART2。切换没有修改 `.ioc`、PD5/PD6、UART4 BSP 或 230400/8N1 配置。
- `commTask` 的字节接收、异步帧发送和两处 UART 统计读取统一使用同一个 `debug_uart_port`；运行快照字段由物理端口绑定的 `uart_ttl` 改为 `debug_uart`，遥测错误汇总随配置端口切换。
- USART2 当前只承载既有大写 ASCII 板测协议，不做 ASCII/X-Protocol 自动探测。M5 启用正式 X-Protocol 前必须先把集中配置迁回 `BSP_UART_TTL/UART4`，最终 STM32—树莓派—电脑层级不变。
- 新增 `app_debug_uart_config` 主机测试，锁定当前 M2 镜像必须选择 USART2；全量主机测试 13/13 通过。Debug/Release 固件全量构建均为 67/67，RAM 均为 47,088 B，Flash 为 85,372/48,424 B；相对上一提交 RAM 不变，Flash 增加 24/8 B。
- `app_tasks.c` 使用 ARM GCC 追加 `-Wextra -Wshadow -Wconversion -Werror -fanalyzer` 检查并通过；`git diff --check` 与调试链硬编码扫描作为提交前复核。Type-C/USART2 实机收发仍未执行，下一动作是在电机动力关闭或车轮完全悬空时接收遥测并发送 `STATUS`。

## 2026-07-18：完成 M2 板测提交一的 USART2 通信冒烟验收

- 测试条件为 ST-Link 3.28 V 单独供电、电机无动力、车轮悬空；重新下载并校验提交 `bfb60a7` 的 Debug ELF 成功。全程只执行被动遥测接收和一条 `STATUS`，未发送 `ARM/PWM/STOP/ESTOP`。
- 原 USB-DEBUG/CH9102 对应的 COM5 无收发；根据 H60 V3.6.0 原理图切换到连接 USART2 PD5/PD6 的 USB-COM 后，Windows 枚举为 COM6。COM6 使用 230400 baud、8N1、无流控，2.2 s 收到 100 条完整 `T,...` 遥测，首尾时间戳单调增长。
- `STATUS\n` 被协议解析器接受，板端 `accepted_command_count` 从 0 增至 1，并按设计请求同格式状态遥测；语法、数字、范围、旧序号和行溢出计数均为 0。
- 连续三次关闭并重开 COM6，每次约 0.9 s 分别收到 40、41、41 条遥测，均立即恢复。测试前后 `uwTick` 从 `0x000379A0` 增至 `0x0003CF11`，确认复开过程中未异常复位。
- 最终板端统计为发送入队 11,269 帧、完成 11,268 帧且 1 帧处于发送队列；UART 奇偶校验、噪声、帧、溢出、接收恢复失败、RX 环形缓冲溢出、TX 队满、TX 启动失败、TX 完成恢复、遥测丢弃和格式化错误均为 0。提交一据此关闭；持续拥塞、错误注入、ADC 标定、电机运动及 M1.3 深度板测仍属于后续提交。

## 2026-07-18：完成 M2 板测提交二——Windows 串口采集工具

- 新增 `tools/capture_serial.ps1` 与纯函数库 `tools/serial_capture_lib.ps1`。工具强制显式提供 COM 口和已烧录固件提交，默认使用 230400/8N1、无流控、DTR/RTS 关闭；每次采集在独立目录生成原始字节日志、40 字段遥测 CSV、命令时间线 CSV 和 JSON 元数据。
- 遥测解析严格检查 52 个线协议 token、固定标签、整数类型边界和系统布尔值。元数据记录固件/仓库提交、仓库脏状态、工具与解析库 SHA-256、PowerShell/OS、串口配置、UTC 起止时间、各类计数和产物名称；失败打开串口时也尽可能保留 `outcome=failed` 的证据目录。
- 默认命令安全门只允许 `STATUS`。定时命令计划必须提供 `elapsed_ms,command`，非 `STATUS` 命令还需显式使用 `-AllowNonStatusCommands`；该开关不代替现场供电、机械和固件安全检查。完整用法与字段说明见 `docs/serial_capture_tool.md`，本地 `captures/` 已忽略，不提交可能较大的原始数据。
- `test_serial_capture.ps1` 覆盖有效帧、字段数/标签/符号/布尔边界、CSV 转义、默认命令拒绝和计划排序，共 15 项断言，并作为 Windows CTest 项接入 `tools/run_host_tests.ps1`。全量主机测试由 13 项增至 14 项且 14/14 通过；Windows PowerShell 5.1 与 PowerShell 7 均通过脚本离线测试。
- 使用 Windows PowerShell 5.1、COM6、固件 `bfb60a7` 和 `-SendStatusAtStart` 完成 5 s 实采，产物位于本地 `captures/20260718-161012922_COM6/`：收到 33,060 B、解析 228 行、无非遥测行、无尾部残片、解析错误为 0；板端时间单调增长，发送的 1 条 `STATUS` 在计划后 10 ms 完成，所有通信、命令队列、运动门、失效命令和 ADC 错误字段均保持 0。
- 本提交不改变固件源代码或 `.ioc`；Debug/Release 仍为 67/67 构建通过，RAM 均为 47,088 B，Flash 分别为 85,372 B 与 48,424 B。提交二据此关闭，后续可直接复用同一采集格式执行 M1.3 深度板测、单电机开环辨识和控制器对比。

## 2026-07-18：完成 M2 板测提交三的无动力试验准备

- 新增 `new_g2_first_motion_plan.ps1` 和 `g2_experiment_lib.ps1`。生成器不访问串口，分别生成 MA 正向或负向首动计划；首动软件临时上限为 200，低于固件 840 上限，并按不超过 400 ms 的间隔插入保活 PWM，避免越过 500 ms 命令超时。
- 每份计划包含待执行 CSV 和 JSON 清单，固定以 `STATUS` 开始/结束，只含一次 ARM/STOP，不包含 `ESTOP`。清单默认 `execution_state=not_authorized`，电机动力、极性与共地、MA 单通道、车轮悬空、紧急断电和人工授权均保持未确认。
- 新增 `analyze_g2_capture.ps1`。分析前强制闭合原始字节、遥测行、命令行及解析计数；随后输出板端采样间隔、控制 tick 差、时序初查、PWM、电池、四路编码器累计差与 counts/s，以及全部通信/ADC 错误最大值。每圈计数和脉冲周期未验证前明确保持 `G3Ready=false`，不生成伪 RPM。
- 使用提交二的 229 行无动力采集完成真实离线验证：板端跨度 4,994 ms，遥测间隔主体为 22 ms；累计最大 IRQ 抖动约为控制周期 0.1405%，最大 WCET 约为 7.0929%，漏周期和截止期增量均为 0。该结果只证明工具链和明显时序阻断项正常，50 Hz 快照不能替代完整 1 kHz P99 分布验收。
- 生成 `dryrun_positive_pwm100` 干跑计划验证落盘格式，共 8 条命令，状态保持 `not_authorized`；整个提交未打开 COM6、未发送 `ARM/PWM/STOP/ESTOP`、未接入电机动力。完整执行门和后续顺序见 `docs/g2_g3_experiment_procedure.md`。
- G2/G3 工具在 Windows PowerShell 5.1 与 PowerShell 7 下均为 19 项断言通过，并作为新 CTest 项把统一主机测试扩展为 15/15。Debug/Release 仍为 67/67，RAM 均为 47,088 B，Flash 分别为 85,372 B 与 48,424 B；本提交未修改固件源代码或 `.ioc`。

## 2026-07-18：完成 MA 正向动力首动检查点

- 板测条件为 3S 电池约 11.7 V、电机动力已接、车轮悬空、COM6/USART2 230400 8N1；所有运动计划均以 `STATUS` 预检开始，以受控 `STOP` 和结束 `STATUS` 收尾。
- `PWM 100` 两次均未观察到电机自主转动。第二次约 600 counts 的变化由操作者手动转轮产生，明确排除为电机首动证据。
- `PWM 200` 首次试验中 MA 编码器增加 12,263 counts，但操作者当时离开现场；观察重试中编码器增加 13,006 counts，操作者确认 MA 车轮连续向后转动。两次试验的 MB/MC/MD 编码器变化均为 0。
- 软件正向当前对应物理向后和编码器正计数；MA 电机、J7/AT8236 输出、TIM1 CH1/CH2、PA15/PB3 编码器与串口命令链路据此完成首次联合验证。反向关系仍需单独板测，不凭对称性假定。
- 当前工况下正向启动阈值只收敛为 `(100, 200]` PWM counts；相对 4,200 满量程分别约为 2.38% 和 4.76%。该区间尚未经过电池状态、温度、承重和重复性扫描，不能直接冻结为控制器死区补偿。
- 被现场确认的重试中电池为 11.692-11.727 V，板端实际 PWM 达到 200 后回到 0；无故障、急停、命令拒绝或 ADC 错误，最终 `motor_state=0`、目标/实际 PWM 均为 0。
- 修正离线分析器的采集边界判定：串口可在帧中间打开或关闭，完整行必须闭合为“遥测行 + 非遥测行”，非遥测首行和尾部残片作为边界元数据保留，不再被误判为内容损坏；原始字节仍为权威证据。

## 2026-07-18：关闭 MA 编码器轮端四倍频歧义

- 使用 COM6/USART2 进行 45 秒纯 `STATUS` 采集，未发送任何运动命令；2,050 行遥测解析错误为 0，包含约 12.8 秒转前静止平台、人工向前三整圈和约 20.9 秒转后静止平台。
- MA 转前/转后平台分别为 -1,084,661 和 -1,456,444 counts，三圈累计 -371,783 counts，平均绝对值 123,927.7 counts/rev；MB/MC/MD 全程累计变化均为 0。
- 实测平均值相对原厂 `1024 PPR × 4 × 30 = 122,880 counts/rev` 高约 0.85%，而相对旧配置 30,720 高约 4 倍。偏差量级与人工标记对齐和停止时约 31 counts 的轻微回摆一致，因此用本试验关闭倍率歧义，不把人工结果替代为精密标定值。
- `robot_config.h` 将 1024 PPR、四倍频因子 4 和减速比 30 分开表达，轮端配置修正为 122,880 counts/rev；新增主机测试锁定三个因子和最终乘积。
- G2 离线摘要把 `counts_per_wheel_revolution_verified` 更新为 true 并记录 122,880，但仍不报告 RPM；脉冲周期数据和 M/T 速度估计实现前，G3 继续保持未完成。
- 统一主机测试由 15 项增至 16 项且 16/16 通过；G2 工具在 PowerShell 7 与 Windows PowerShell 5.1 下均为 21 项断言通过。Debug/Release 固件全量构建均为 67/67，RAM 均为 47,088 B，Flash 分别为 85,372 B 与 48,424 B；本次只修正尚未参与当前开环执行的机械换算配置，因此资源占用不变。

## 2026-07-18：完成 MA 悬空死区编码器自动二分

- 新增 `new_g2_deadzone_probe_plan.ps1`，在不削弱首动计划 200 上限的前提下单独支持 1-840 的死区探测计划；仍固定单方向、一次 ARM/STOP、400 ms 内保活、默认未授权且不打开串口。
- 新增 `classify_g2_deadzone_capture.ps1` 和 `Measure-G2DeadzoneMotion`。默认要求 MA 沿期望符号累计位移至少 1,000 counts，反向和其他通道均未越界，并同时核对最终停机状态与全部通信/ADC 错误；几十至数百 counts 的抖动不再依赖肉眼判断。
- 首次带电预检只测得 4.175-4.192 V，安全流程在任何运动命令前暂停；重新接通动力后预检恢复到 11.709-11.718 V，运行许可、停机状态和全部错误计数正常，才继续试验。
- 正向二分结果：`+150` 最大期望位移 136，判定未起转；`+175` 首次/复验分别为 7,433/13,171，均可靠起转，悬空区间收敛为 `(150, 175]`。
- 反向二分结果：既有 `-200` 两次未起转；`-520/-360/-280/-240/-220` 分别产生 53,729/36,799/27,380/22,839/17,840 counts 的期望负向位移，`-220` 复验为 17,318，悬空区间收敛为 `(200, 220]`。
- 所有二分和复验采集最终目标/实际 PWM 与 `motor_state` 均为 0，其他三个编码器最大位移不超过 1 count，电池最低 11.701 V，无故障、急停、命令拒绝、运动门拒绝或 ADC 错误。
- 统一主机测试 16/16 通过；G2 工具在 PowerShell 7 与 Windows PowerShell 5.1 下均为 28 项断言通过。死区工具和文档不修改固件源代码或 `.ioc`，Debug/Release 资源结果沿用提交 `0ba2d37` 的 67/67、RAM 47,088 B、Flash 85,372/48,424 B。

## 2026-07-18：完成 MA 悬空双向多档工作点初扫

- 新增 `analyze_g2_operating_point.ps1` 和 `Measure-G2OperatingPoint`，只在目标/实际 PWM 同时达到指定幅值并排除最初 500 ms 收敛段后的窗口统计稳态 counts/s，并使用已验证的 122,880 counts/rev 离线换算平均轮端 RPM；同时输出 PWM 爬升、1,000 counts 运动阈值延迟、带载电压和其他通道位移。
- 工作点分析继续执行完整证据闭合与安全门：采集必须完成且零解析错误，MA 符号必须正确，MB/MC/MD 不得越界，最终目标/实际 PWM 和电机状态必须归零，通信、命令、安全门和 ADC 错误必须全部为 0。
- 在约 11.7 V、车轮悬空、每档约 3 s 条件下完成 `±240/±400/±600/±840` 八个工作点。正向平均轮端速度依次为 13.093/25.068/40.290/59.041 RPM，反向幅值依次为 11.031/22.459/37.650/56.039 RPM。
- 四点线性拟合得到正向 `RPM ≈ 0.076598×PWM - 5.4579`、`R²≈0.99991`，反向幅值 `RPM ≈ 0.075202×|PWM| - 7.3103`、`R²≈0.99979`。同幅值下正向快约 2.06-3.00 RPM；该结论只用于识别后续重复性和方向补偿需求，不外推到死区或承重工况。
- 八次采集的最低电压为 11.656 V，其他三个编码器最大位移为 0；每次均受控回到安全停机且无故障、急停、命令拒绝或 ADC 错误。最终独立 `STATUS` 复核为电池 11.701 V、目标/实际 PWM 0、`motor_state=0`、全部错误 0。
- G2 工具在 PowerShell 7 与 Windows PowerShell 5.1 下均为 35 项断言通过。本次固件仍为板上提交 `ad0fd37`，源码当前 122,880 counts/rev 仅用于主机侧换算；工作点扫描不依赖固件内机械参数。G2/G3 尚未关闭，后续仍需每档至少三次、承重/多电压、阶跃/斜坡/零点穿越、完整时序和 M/T 测速数据。

## 2026-07-18：完成 MA 悬空多档重复性筛选

- 动力异常排查全程先走纯 `STATUS`：两次采集曾收到 0 字节，通信恢复后的首份完整采集又只读到最低 4.139 V，因此未发送任何运动命令。随后两次独立预检恢复为 11.674-11.727 V，并同时满足运行就绪、停机、零故障/急停和全部错误为 0，才放行重复性试验。现有串口证据不能进一步区分动力插头、开关、保险或共地瞬断。
- 保持板上固件 `ad0fd37`、122,880 counts/rev 主机离线换算、MA 单通道和车轮悬空，按顺序完成第二轮八档，冷却超过 60 s 并纯 `STATUS` 复核后完成第三轮反序八档。18 次新增运动采集全部方向正确、其他三通道最大位移 0、最终安全停机、全部错误为 0；最低带载电压 11.639 V，最大基线压降约 38 mV。
- 新增 `Measure-G2Repeatability` 与 `summarize_g2_repeatability.ps1`，按方向/PWM 汇总均值、样本标准差、CV、最大单次偏离、最低电压和单调性。首轮 24 次汇总中反向 240 的 `CV=7.831%`，按门槛补两次复验后，26 次最终汇总为 7/8 组通过且双向单调，但反向 240 五次仍为均值 12.162 RPM、`CV=6.540%`、最大单次偏离 9.295%，总筛选保持未接受。
- 反向 240 位于已测悬空死区 `(200,220]` 上沿附近，当前结果只支持“低速离散待关闭”，不支持动态建模或冻结死区补偿。后续应把机械静摩擦、温度/停放姿态和试验顺序作为受控变量，不继续无上限追加同一工况。
- 最终独立纯 `STATUS` 为 11.647-11.683 V、运行就绪、目标/实际 PWM 0、`motor_state=0`、故障/急停/全部错误为 0；运动阶段安全结束。
- 重复性工具在 PowerShell 7 与 Windows PowerShell 5.1 下均为 42 项断言通过，全量主机测试 16/16 通过，`git diff --check` 无错误；新增汇总器和工作点分析入口统一使用 UTF-8 BOM，真实 26 份清单及单档摘要均已直接通过 Windows PowerShell 5.1。本阶段未修改固件源码或 `.ioc`，板上继续保持 `ad0fd37`。

## 2026-07-19：完成反向 240 固定初始轮位诊断

- 操作者给 MA 车轮设置实体标记，每次从同一方向靠近并对齐同一位置；第一次试运行按操作者要求排除，随后以新序号 475/489/503 从头执行三次反向 240。
- 三次结果为 12.522/12.939/13.078 RPM，均值 12.847 RPM、样本标准差 0.289 RPM、`CV=2.253%`、最大单次偏离 2.525%，固定轮位分组由重复性工具判定为通过。
- 三次采集均方向正确、其他三个编码器无越界、最低带载电压 11.639 V、最终安全停机且全部错误为 0；最终纯 `STATUS` 为 11.639-11.656 V、目标/实际 PWM 0、`motor_state=0`。
- 固定轮位把离散从原五次 `CV=6.540%` 降到 2.253%，说明初始机械相位或回差是重要变量，但因只覆盖一个轮位，原全局重复性门仍不关闭。下一步做多轮位对照，不直接进入动态建模。

## 2026-07-19：完成反向 240 多轮位对照

- 操作者确认 120° 前五次的实体标记可能未贴紧；这些采集保留但从正式统计中排除。重新固定标记后的后三次为 12.076/12.663/12.753 RPM，均值 12.497 RPM、`CV=2.942%`、最大偏离 3.372%，作为有效 120° 组。
- 有效 0°/120°/240° 三组均值分别为 12.847/12.497/12.408 RPM，组内 CV 为 2.253%/2.942%/2.898%；9 次主对照整体均值 12.584 RPM、`CV=2.835%`、最大单次偏离 4.267%，组均值极差约 3.481%，全部通过既定门槛。
- 全部有效与被排除运动采集均方向正确、其他通道无越界、最终安全停机且全部错误为 0，最低带载电压 11.603 V；最终纯 `STATUS` 为 11.603-11.647 V、目标/实际 PWM 0、`motor_state=0`。
- 修正后关闭已选三个悬空轮位的反向 240 重复性门，不再保留“机械角位置显著敏感”的结论。下一步进入阶跃/斜坡和零点穿越数据采集；承重、多电压、温升与 Brake/Coast 未完成前仍不进入动态建模。

## 2026-07-19：完成 M2 板测提交四的软件——板内 1 kHz 电机高速采集

- 新增无 HAL/RTOS、无动态内存的 `AppMotorCapture`。控制任务在电机输出提交后、周期 WCET 结束计时前记录 2200 个固定样本；满缓冲后停止并报告丢弃，不覆盖旧数据。每个样本包含完整 IRQ/唤醒/WCET 时序证据、MA 编码器、目标/实际 PWM、电池、电机状态和安全位。
- 高速缓冲通过独立 `NOLOAD .motor_capture` 链接段放入 64 KB CCMRAM；CPU 在停车后逐条复制到既有 USART2 异步发送队列，不把 DMA 不可访问的 CCMRAM 直接交给外设。Debug/Release 均使用主 RAM 47,120 B、CCMRAM 61,612 B；Flash 分别为 89,936/50,424 B。
- 临时 ASCII 协议新增 `CAPTURE START/STOP/STATUS/EXPORT`。START 和 EXPORT 都复核 MA 完全停机，导出包含版本化 BEGIN/END 与连续索引；通信任务每轮最多入队一帧并在队满前主动背压，仍持续服务 ESTOP、普通命令和50 Hz状态遥测。
- Windows 采集格式升级到 schema 2，新增高速样本/事件 CSV 和严格解析计数；新增无动力时序计划生成器、完整 1 kHz 时序分析器及 PowerShell 5.1/7测试。统一主机测试扩展为18项并全部通过；串口解析26项断言、高速分析与计划15项断言均在两个 PowerShell 版本通过，三个主要受影响 C 文件通过严格告警和 GCC `-fanalyzer`。
- 本提交尚未烧录开发板，也未接入电机动力。下一动作是生成默认未授权的无动力计划，保持电机动力关闭，在 COM6/USART2 上验证约2000个样本的 BEGIN/END 闭合、索引/tick连续性、零丢弃、完整P99/最大抖动、唤醒延迟和WCET门槛。

## 2026-07-19：完成板内 1 kHz 高速采集无动力验收

- ST-Link 正常识别 STM32F407，目标电压为 3.28 V；将提交 `57cbcab` 的 Debug ELF 烧录、写后校验并复位成功。电机动力保持关闭，板载 USART2 USB-UART 继续枚举为 COM6。
- 先执行 10 s 纯 `STATUS` 安全门：收到 60,568 B、解析 456 行遥测且错误为 0；运行就绪、MA `DISARMED`、目标/实际 PWM 0，漏周期、截止期、UART 错误/溢出/发送故障均为 0。ADC 为 4.192 V 左右，只记为无动力基线，不放行运动。
- 执行不含 `ARM/PWM/STOP/ESTOP` 的零输出高速计划，完整导出 2019 个样本和 6 个状态事件；BEGIN/END 与 CSV/元数据闭合，索引和控制 tick 均连续，丢弃、漏周期、截止期和编码器相对变化均为 0。
- 全样本分析得到 IRQ P99/最大抖动 5/6 cycles，唤醒 P99/最大延迟 2744/2745 cycles，最坏上一周期 WCET 12238 cycles；分别占 1 kHz 周期的 0.003%、1.63% 和 7.28%，全部通过 P99 5%、最大值 10%、WCET 25% 门槛。
- 本地原始证据位于 `captures/20260719-123141561_COM6/` 与 `captures/20260719-123222494_COM6/`，按仓库规则保持忽略。无动力提交四验收关闭；下一步是接通电机动力后的独立纯 `STATUS` 电压预检，通过后再规划首个低功率动态高速采集。

## 2026-07-19：首次动态高速采集发现并修复 UART 导出竞争

- 带动力 10 s 预检与动作前 3 s 即时预检分别得到 456/137 行零错误遥测，动力范围为 11.603-11.647 V，MA `DISARMED`、运行就绪且全部安全门通过。随后执行一次软件反向 `PWM -240`、约 1 s 的 MA 悬空动态计划。
- 动作完整记录 2004 个连续 1 kHz 样本，MA 累计 -14358 counts，其他三个编码器无变化；目标与实际 PWM 最终归零、`motor_state=0`、动力最低 11.621 V。IRQ P99/最大抖动为 135/558 cycles，唤醒 P99/最大延迟为 2721/3384 cycles，最坏上一周期 WCET 为 14296 cycles，时序门全部通过。
- 导出期间 `uart_tx_fault_count` 从 0 增到 17，故该次动态数据明确拒绝验收。代码审查确认高速导出先填满 4 槽发送队列后，下一帧 50 Hz 遥测会直接入队失败并累计 `tx_queue_full_count`；完整样本与 BEGIN/END 不能抵消通信故障。
- 修复将高速导出占用上限改为 3 槽，固定为状态遥测保留 1 槽，并用编译期约束锁定预留值。高速分析 schema 升级为 2，除漏周期/截止期外，新增检查 UART、命令队列、运动门和 ADC 全部计数增量；真实故障采集已被更新后的分析器以增量 17 正确拒绝。
- 统一主机测试 18/18 通过；高速分析脚本在 Windows PowerShell 5.1 下 16 项断言通过，新增合成故障回归确认 UART 发送故障必定拒绝。Debug/Release 全量构建通过，资源保持 RAM 47,120 B、CCMRAM 61,612 B、Flash 89,936/50,424 B；`app_tasks.c` 通过严格告警和 GCC `-fanalyzer`。
- 下一步必须先断开电机动力，烧录修复提交并重复零输出高速导出。只有全量样本、时序和新增严格错误门均通过，才重新接通动力复测 `-240` 动态计划。

## 2026-07-19：通过 UART 队列预留修复的无动力复验

- 在电机动力关闭、ST-Link 目标电压 3.28 V 条件下烧录并校验修复提交 `cc1b43a`。复位后 5 s 纯 `STATUS` 得到 227 行零错误遥测，MA 停机、运行就绪且全部计数从 0 开始；无动力 ADC 为 4.104-4.121 V。
- 零输出高速计划完整导出 2003 个样本和 6 个事件，索引/tick 连续、缓冲丢弃 0、编码器相对变化 0。IRQ P99/最大抖动为 5/6 cycles，唤醒 P99/最大延迟为 2744/2745 cycles，最坏上一周期 WCET 为 12238 cycles，全部时序门通过。
- schema 2 严格门确认漏周期、截止期、UART 错误/溢出/发送故障、命令拒绝/队列丢弃、运动门拒绝、失效命令和 ADC 错误增量全部为 0，最终 `accepted=true`。之前动态导出中的 17 次 UART 发送故障未复现，证明预留一个遥测槽位在无动力导出下有效。
- 本地证据位于 `captures/20260719-124911014_COM6/` 与 `captures/20260719-124944124_COM6/`。下一步重新接通动力并先过独立纯 `STATUS`，再复测相同 `PWM -240` 动态计划；首份被拒绝数据仍不得进入模型拟合。

## 2026-07-19：通过 UART 修复后的带动力动态复验

- 重新接通 3S 动力后，5 s 纯 `STATUS` 得到 228 行零错误遥测；电压 11.621-11.647 V，MA 停机、PWM 0、运行就绪，全部 UART、命令、安全门和 ADC 计数为 0。
- 使用修复固件 `cc1b43a` 复测同一软件反向 `PWM -240` 计划，完整导出 2002 个 1 kHz 样本和 6 个事件。MA 累计 -19099 counts，其他三个编码器变化均为 0；目标、实际 PWM 和 `motor_state` 最终归零，动力范围为 11.621-11.639 V。
- 239 ms 斜坡达到 -240，首个负向编码器增量相对目标约延迟 86 ms，STOP 后 239 ms 回到 0。约 759 ms 的满 PWM 窗口均值为 -20498 counts/s，即 -10.009 RPM；该短窗口只用于核对数据形态，不代替既有 3 s 稳态工作点，也不直接参与模型拟合。
- IRQ P99/最大抖动为 334/782 cycles，唤醒 P99/最大延迟为 2721/3353 cycles，最坏上一周期 WCET 为 12661 cycles；索引/tick 连续、缓冲丢弃 0，全部 UART、命令、运动门、ADC、漏周期和截止期增量为 0，schema 2 严格分析 `accepted=true`。
- 本地正式证据位于 `captures/20260719-125148868_COM6/` 与 `captures/20260719-125240141_COM6/`。板内 1 kHz 采集设施的无动力与带动力门均关闭；下一阶段进入成组阶跃/斜坡、零点穿越和 Brake/Coast 计划设计，仍需每次独立动力预检和停车后导出。

## 2026-07-19：完成 G2 受控动态阶跃离线工具

- 新增单次动态阶跃计划生成器和纯分析库。计划固定为 14 s、13 条命令，只允许 240/400/600/840 PWM 的单方向动作；不含反转或 ESTOP，明确保留固件 `1 count/ms` 斜坡，并把目标命令与板内 1 kHz 实际 PWM 分开核对。
- 新增批次生成器，默认生成悬空 `±400` 各三次试点，奇数轮正向先行、偶数轮反向先行；每个子计划使用互不重叠的命令序号，批次和子计划默认均为 `not_authorized`，要求逐计划纯 `STATUS`、明确授权、30 s 冷却和任一拒绝即中止。
- 新增 `analyze_g2_dynamic_step.ps1`。分析器复用 schema 2 高速门，并严格核对固件提交、计划/实发命令、最大 100 ms 调度误差、实际 PWM 符号/峰值/升降斜坡、基线与峰值样本、MA 运动方向、其他通道、最低动力电压、最终停车和全部错误计数；单次通过也固定保持 `model_ready=false`。
- G2 动态阶跃专项在 PowerShell 7 和 Windows PowerShell 5.1 下均为 18 项断言通过，5 个相关脚本在两个版本下均无解析错误，统一主机测试扩展为 19 项且 19/19 通过。Debug/Release 增量构建通过且无需重编；本切片未修改固件源代码或 `.ioc`，尚未接通动力或执行新一轮板测，差异审查后再生成首组正式板测计划。

## 2026-07-19：完成首组悬空中档动态阶跃试点

- 使用板上固件 `cc1b43a` 按正、反、反、正、正、反顺序执行 `±400` 各三次。每次均先完成独立 5 s 纯 `STATUS`，动力范围覆盖 11.585-11.674 V，MA 停机、运行就绪且全部错误为 0；操作者逐次确认只有 MA 正常运动，无卡滞、异响、异常振动或其他车轮动作。
- 六次分别导出 2000/1996/2004/1985/2004/1997 个高速样本，13 条命令均完整发送，升降斜坡均为 399 ms，其他三个编码器均为 0，最终全部安全停机。六次 schema 2 和动态严格门均 `accepted=true`；全批次索引/tick 零断点，最坏 IRQ 抖动 782 cycles、最大唤醒延迟 3430 cycles、最坏 WCET 13722 cycles，全部 UART、命令、安全门、ADC、漏周期和截止期增量为 0。
- 正向峰值窗口为 18.922/20.701/20.852 RPM，均值 20.158 RPM、`CV=5.327%`；反向为 17.335/18.673/17.793 RPM，均值 17.934 RPM、`CV=3.791%`。正向比反向平均高 2.225 RPM，约占双向均值的 11.68%；全批次最低动力电压 11.594 V。
- 新增可复现批次汇总函数和 `summarize_g2_dynamic_step_batch.ps1`，分别报告采集证据接受、重复性筛选和模型就绪状态。实际结果为 `capture_evidence_accepted=true`、`repeatability_accepted=false`、`model_ready=false`：反向通过，正向按 5%-10% CV 规则补两次，不能因六份单次证据均接受而提前拟合模型。
- 动力断开后的 5 s 纯 `STATUS` 得到 228 行零错误遥测，ADC 回到 3.856-3.891 V 无动力基线，MA 停机且 PWM 为 0。批次汇总专项在 PowerShell 7 和 Windows PowerShell 5.1 下均为 23 项断言通过；下一步在保持同一机械配置和冷却规则下补两次正向 400，再重新计算五次正向统计。

## 2026-07-19：通过正向 400 动态补测并关闭中档重复性门

- 在每次独立纯 `STATUS` 确认 11.603-11.621 V、停机和零错误后，执行正向 400 第四、第五次。两次分别得到 1993/1998 个高速样本、35,778/38,254 counts 位移和 19.457/20.827 RPM 峰值窗口，升降斜坡均为 399 ms，其他通道均为 0；最低动力电压 11.576 V，单次严格分析和现场观察均通过。
- 合并原三次后，正向五次峰值窗口均值 20.152 RPM、样本标准差 0.901 RPM、`CV=4.469%`、最大单次偏离 6.104%，从补测区间降到通过；反向三次保持均值 17.934 RPM、`CV=3.791%`。正向均值比反向高 2.218 RPM，约占双向均值的 11.65%。
- 批次汇总工具新增独立补测目录与计划清单绑定，防止把来源不明的额外摘要混入初始批次。八次正式结果为 `capture_evidence_accepted=true`、`repeatability_accepted=true`、`model_ready=false`；中档重复性门关闭，但完整多档辨识和独立验证集仍未完成。
- 最终动力断开纯 `STATUS` 得到 228 行零错误遥测，ADC 为 3.758-3.820 V，MA 停机、PWM 0。补测绑定专项在 PowerShell 7 和 Windows PowerShell 5.1 下均为 24 项断言通过；下一步离线准备其余幅值动态计划，不立即接通动力。

## 2026-07-19：连续幅值试验在反向 240 异常处中止

- 连续计划保持逐次 5 s 纯 `STATUS`、14 s 动态采集和 30 s 冷却。`±600` 六次全部接受，正向均值 35.061 RPM、`CV=3.402%`，反向均值 31.169 RPM、`CV=2.175%`；`+240` 三次均值 11.412 RPM、`CV=1.766%`，均关闭对应重复性门。
- `-240` 前两次为 8.825/9.180 RPM、16,338/17,112 counts 和 227/236 ms 延迟，第三次降到 1.084 RPM、3,399 counts、906 ms。该次仍越过旧单次 1,000 counts 门而被单项接受，但跨重复数量级变化明确异常。
- 外部发现后要求立即断电，精确终止自动测试进程并直接发送 `ESTOP`。因主机输出存在延迟，停止生效前完整执行了一次 `+840`；该次为 51.369 RPM、95,614 counts、最低 11.594 V，单次严格门通过。下一次 `-840` 的临时采集目录没有元数据且 `commands.csv` 为 0 字节，证明没有发送运动命令。
- ESTOP 后板端为 PWM 0、`motor_state=6`、故障和急停锁存。操作者复位后只执行一次独立 `-240` 补测，恢复到 9.473 RPM、17,407 counts、249 ms 并严格接受；保留异常样本后四次反向统计仍为峰值 `CV=56.669%`、位移 `CV=50.071%`、延迟 `CV=82.684%`，结果阻断而非通过。
- 新增 `Measure-G2DynamicCrossRepeatGate` 和 `check_g2_dynamic_cross_repeat.ps1`。同工况至少两次历史接受结果后，新结果的峰值 RPM/位移必须在历史中位数 50%-150%，延迟不得超过 2 倍；真实异常被拒绝、正常补测被接受。专项在 PowerShell 7 和 Windows PowerShell 5.1 下均为 26 项断言通过。
- 最终动力关闭纯 `STATUS` 为 3.802-3.856 V，MA 停机、ESTOP/故障清除且全部错误为 0。840 批次继续暂停；下一步先完成跨重复门的统一主机验证和文档提交，再决定反向 240 的机械条件诊断。

## 2026-07-19：重新建立反向 240 正式重复性组

- 按操作者明确要求，原连续流程的三次 `-240` 和复位后一次补测不再纳入本轮正式重复性统计；这里的“排除”只改变统计集合，不删除原始采集，也不撤销异常、自动流程中止和 ESTOP 处置记录。
- 使用固件 `cc1b43a` 和仓库基线 `412dab7` 生成三个独立、默认未授权的全新反向 240 计划。每次均经过纯 `STATUS`，三次动态采集分别位于 `captures/20260719-152304066_COM6/`、`captures/20260719-152525386_COM6/` 和 `captures/20260719-152818633_COM6/`。
- 三次均完整发送 13 条计划命令并导出 2008/1992/2008 个高速样本；MA 位移为 14,307/15,486/16,634 counts，峰值窗口为 8.120/8.754/9.126 RPM，运动阈值延迟为 380/276/252 ms。升降斜坡均为 239 ms，MB/MC/MD 变化均为 0，全部严格错误计数为 0，最低动力电压为 11.585 V。
- 三次单次严格门均 `accepted=true`；第三次相对前两次新基线的跨重复在线门也通过。只汇总这三次时，峰值 RPM 均值为 8.667、`CV=5.869%`，位移 `CV=7.518%`，延迟 `CV=22.480%`，因此筛选结果为 `retest` 而不是 `pass`，`model_ready=false`。
- 动力接通后的结束纯 `STATUS` 为 11.585-11.603 V；操作者断开动力后复核为 3.935-3.997 V。两次均确认目标/实际 PWM、`motor_state`、ESTOP 和故障为 0，运行就绪且全部通信、命令、安全门、ADC、漏周期和截止期计数为 0。下一次运动仅允许补两次反向 240 并按五次整体重算，不恢复 840 批次。

## 2026-07-19：反向 240 补到五次后仍未关闭

- 在仓库提交 `bf9a27a` 下执行第四、第五次独立反向 240 计划。两次动作前纯 `STATUS` 分别为 11.585-11.612 V 和 11.576-11.647 V，均确认停机、运行就绪和全部错误为 0。
- 第四次导出 1,988 个高速样本，位移 13,157 counts、峰值 7.405 RPM、运动阈值延迟 385 ms；第五次导出 2,006 个高速样本，位移 16,162 counts、峰值 8.722 RPM、延迟 244 ms。两次均完整发送 13 条计划命令，升降斜坡均为 239 ms，MB/MC/MD 变化为 0，单次严格门和相对全部新基线的跨重复门均通过。
- 保留前三次并汇总五次，峰值 RPM 均值 8.425、样本标准差 0.675 RPM、`CV=8.009%`、最大单次偏离 12.112%；位移均值 15,149.2 counts、`CV=9.348%`，延迟均值 307.4 ms、`CV=22.636%`。工具筛选仍为 `retest`，但批次已达到五次上限，因此工程门保持未关闭，禁止继续无上限追加、恢复 840 或进入模型拟合。
- 上电结束纯 `STATUS` 为 11.576-11.603 V；操作者断开动力后复核为 3.891-3.926 V。两次均为目标/实际 PWM 0、`motor_state=0`、ESTOP/故障 0 和全部错误计数 0。下一步只离线设计初始轮位、靠近方向、静置时间和温度可记录的机械条件诊断，评审后再决定是否需要新的板测。

## 2026-07-19：同步旧路线与当前执行边界

- 复核从旧基线 `bfb60a7` 到当前 `24b40d4` 的 22 个提交，确认 USART2 通信和 Windows 采集工具已关闭，板内高速采集只完成电机模式，原路线要求的 `STAT/IMUQ/RES/EVENT` 分类型遥测、IMU 高速模式及完整 M1.3 联合板测尚未完成。
- 更新 `task_plan.md`：G1.1-G1.3 改为“部分板测完成”，分别列出已验证证据与仍缺的资源水位、故障注入和通信压力项；明确历史命名的“M2 板测提交三”不等于原路线的分类型遥测提交。
- 更新 `m2_control_architecture_gate.md` 的当前执行边界，删除“先做无动力高速时序、再进入 G2”的过期顺序，改为反向 240 五次仍未关闭、840/拟合暂停以及先离线设计受控启动与稳定速度诊断。此次只同步文档，不修改固件、`.ioc`、采集数据或测试结论。

## 2026-07-19：完成反向 240 启动与稳定速度离线拆分

- 新增 `g2_low_speed_diagnostic_lib.ps1` 和 `analyze_g2_low_speed_diagnostic.ps1`，固定使用平台结束前 200/300 ms 两个窗口，分别报告首个期望编码器增量延迟、1,000 counts 延迟、末端速度统计和证据角色。探索性数据即使通过也不能冒充独立验证，且不会追溯关闭原短窗口门或宣称模型就绪。
- 对五次全新 `-240` 重新分析：原短窗口均值 8.425 RPM、`CV=8.009%`；末端 200 ms 为均值 9.328 RPM、`CV=3.674%`、最大偏离 5.224%，末端 300 ms 为均值 9.340 RPM、`CV=4.393%`、最大偏离 6.998%，双窗口均通过预设稳定速度门。首动延迟均值 84.8 ms、`CV=11.566%`，1,000 counts 延迟均值 307.4 ms、`CV=22.636%`，因此探索性分类为 `stable_speed_variable_startup`。
- 新增 `new_g2_low_speed_validation_batch.ps1`，预注册三次独立反向 240 验证。计划把 STOP 从 1800 ms 延至 2300 ms，使满 PWM 平台预计从约 0.71 s 增至 1.211 s；固定 200/300 ms 双窗口、5% CV、10% 最大单次偏离、60 s 冷却、0° 标记同向靠近及 11.500-11.750 V 预检范围，默认 `not_authorized`，任一单次或跨重复门失败立即中止。
- 新增 PowerShell 7/5.1 兼容专项测试并注册统一 CTest。真实分析输出保存在忽略目录 `experiments/generated/g2_low_speed_exploratory_20260719/`；本提交不修改固件或 `.ioc`，独立验证尚未上板。
- 操作者随后决定关闭本批次测温基线。计划生成器和清单明确设置“不测温、不记录温度、温度不参与接受门”，删除“温度值或无工具说明”要求；60 s 冷却、机械标记、靠近方向和动力电压门保持不变。该简化降低操作负担，但本批次结论不得外推为跨温度稳定。

## 2026-07-19：关闭反向 240 独立验证的单次分析链

- 操作者完成 0° 对齐和静置后，上板前复核发现新 14 条低速清单与只接受旧动态类型的单次分析器不兼容。保持动力断开，先修复工具链，未发送 `ARM/PWM`。
- 单次严格分析入口现按清单类型校验旧 13 条动态计划或新 14 条低速计划。低速清单显式冻结 122880 counts/rev、11.500 V 最低带载电压、1,000 counts 运动/其他通道门和至少 1200 ms 实际满 PWM 平台，并在单次摘要直接输出固定末端 200/300 ms 速度。
- 低速专项在 PowerShell 7 与 Windows PowerShell 5.1 下均为 24 项断言通过，既有动态阶跃专项 26 项断言通过；未知试验类型和缺失清单参数继续拒绝。该切片不修改固件或 `.ioc`，下一步在提交推送并生成绑定新仓库提交的三次计划后，才允许通知操作者接通动力并执行纯 `STATUS`。

## 2026-07-19：拒绝首次低速工具确认采集并延长停车后记录

- 上电纯 `STATUS` 得到 229 行零错误遥测，动力为 11.576-11.603 V、停机且运行就绪。随后首次低速工具确认采集完整发送 14 条命令并导出 2000 个连续高速样本；MA 位移 22,687 counts，满 PWM 平台 1215 ms，末端 200/300 ms 为 8.921/8.815 RPM，最低动力电压 11.585 V，其他三路为 0。
- 严格分析唯一失败项为 `final_state_stopped_and_ready`。`CAPTURE STOP` 实际约在 2755 ms，高速末行虽然目标/实际 PWM 已归零，但 `motor_state=ARMED`；最终遥测约到 2847 ms 才完全 DISARMED。该采集保留在 `captures/20260719-164709049_COM6/` 作为被拒绝的工具确认数据，不进入三次独立统计。
- 计划把 `CAPTURE STOP` 从 2750 ms 延至 2900 ms、`CAPTURE STATUS/EXPORT` 相应延至 3150/3400 ms，使记录覆盖 STOP 后 500 ms 自动解锁及至少 50 ms 余量；预计记录 2150/2200 个样本，未扩大固件缓冲。操作者已断开动力，修正版完成测试、提交和重新预注册前不再上电动作。

## 2026-07-19：反向 240 修正版独立验证第 1/3 次通过

- 动作前纯 `STATUS` 得到 226 行零错误遥测，动力 11.585-11.603 V、完全停机且运行就绪。使用仓库提交 `57e474a` 生成的修正版计划执行正式第 1 次。
- 采集 `captures/20260719-170041514_COM6/` 完整发送 14 条命令并导出 2154 个连续高速样本。MA 位移 28,278 counts，首动/1,000 counts 延迟 79/215 ms，实际满 PWM 平台 1213 ms，末端 200/300 ms 为 10.498/10.361 RPM，最低动力电压 11.585 V。
- 高速末行和最终遥测均完全 DISARMED，其他三个编码器为 0，schema 2、命令调度、斜坡、方向、动力和全部错误计数严格门通过。该结果只计为三次独立验证中的第 1 次；操作者已断开动力，重新对齐和至少 60 s 静置后才允许第 2 次。

## 2026-07-19：反向 240 修正版独立验证第 2/3 次通过

- 动作前纯 `STATUS` 得到 227 行零错误遥测，动力 11.585-11.603 V、完全停机且运行就绪。第 2 次采集 `captures/20260719-170708129_COM6/` 完整发送 14 条命令并导出 2162 个连续高速样本。
- MA 位移 26,596 counts，首动/1,000 counts 延迟 104/355 ms，实际满 PWM 平台 1209 ms，末端 200/300 ms 为 10.535/10.552 RPM，最低动力电压 11.585 V。高速末行完全 DISARMED，其他三个编码器和全部严格错误门通过。
- 前两次末端速度接近，但正式 CV、最大单次偏离和启动延迟分类只能在固定第三次后统一计算。操作者已断开动力；重新对齐和至少 60 s 静置后只执行最后第 3 次，不追加第四次。

## 2026-07-19：反向 240 第三次低响应使独立验证未接受

- 第三次动作前纯 `STATUS` 得到 228 行零错误遥测、11.585-11.603 V。采集 `captures/20260719-171159371_COM6/` 完整发送 14 条命令并导出 2144 个高速样本，满 PWM 平台 1212 ms，其他三个编码器和全部错误计数为 0。
- MA 仅移动 2728 counts，末端 200/300 ms 为 0.957/0.638 RPM，1,000 counts 延迟 1485 ms。相对前两次中位数，整窗速度仅 1.84%、位移 9.94%、延迟 5.21 倍，跨重复门拒绝。
- 复核发现单次门把 STOP 后减速阶段才达到 1,000 counts 也算作通过。现将 `expected_motion_threshold_reached` 限定为必须在目标 STOP 前达到，并增加明确摘要字段；PowerShell 7/5.1 回归新增“仅 STOP 后达到阈值必须拒绝”。重跑后第三次 `accepted=false`，正式三次批次为 `insufficient_or_rejected`、`independent_validation_accepted=false`、`model_ready=false`。

## 2026-07-19：操作者要求的反向 240 补充复验

- 操作者要求立即重测第三次。首次补充纯 `STATUS` 只有 3.989-4.006 V，虽已口头确认动力接通，仍按硬门阻止全部运动；重新接通后预检恢复为 11.585-11.594 V。
- 使用全新序号执行的补充采集 `captures/20260719-171944746_COM6/` 得到 2148 个样本、24,831 counts、1218 ms 满 PWM 平台、末端 200/300 ms 10.300/10.285 RPM，最低动力电压 11.585 V，单次和跨重复门均通过。
- 前两次与补充复验组成的后验替换集合末端 200/300 ms 均值为 10.444/10.399 RPM、`CV=1.207%/1.322%`，但这是看过失败结果后的探索性集合，固定输出 `independent_validation_accepted=false`。结束上电纯 `STATUS` 为 11.585-11.594 V；动力断开后复核为 3.829-3.873 V，均停机、运行就绪且错误为 0。
- 离线按目标保持区每 100 ms 分箱：正常第 1/2 次和补充复验在稳定段约为 1600-2180 counts/100 ms；异常第三次依次只有 14/51/23/26/1 counts，随后连续约 0.9 s 为 0，STOP 附近才出现少量计数。四次平均动力电压和最大唤醒延迟没有对应异常，故下一步转入断电机械/连接检查和 G4 电流反馈决策，不再追加无额外观测的相同 `-240` 计划。
- 操作者随后在断电状态手动转动 MA 一整圈，确认阻力均匀且无明显卡涩。持续性粗大机械卡死据此降低优先级，但带扭矩间歇问题、驱动输出、动力连接和编码器链路仍未关闭；G2/G3 保持阻断。
