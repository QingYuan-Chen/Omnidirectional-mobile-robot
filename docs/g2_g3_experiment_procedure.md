# M2 G2/G3 单电机试验流程

## 当前阶段

本阶段先完成不需要电机动力的试验计划和分析工具。只有在电机动力供电、接线和机械安全条件由操作者确认后，才允许把待执行计划交给串口采集工具。

当前仍不能宣称 G2/G3 完成：

- MA 已完成双向首动、悬空死区二分、240-840 PWM 双向工作点扫描和悬空重复性筛选，但承重、多电压、温升与动态工况仍待验证；
- MA 三圈实测已关闭 1024 PPR 的四倍频歧义，轮端采用 122,880 counts/rev；
- 当前遥测只提供原始计数、单周期增量和累计计数，不提供未经验证的 RPM；
- 当前没有脉冲周期数据，不能完成 T 法或 M/T 融合评审；
- 50 Hz 遥测快照只能做初查；2200 样本板内 1 kHz 电机高速记录已通过无动力和带动力严格验收，可用于后续受控动态数据采集。

## 首动计划生成

`new_g2_first_motion_plan.ps1` 只生成文件，不打开串口：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\new_g2_first_motion_plan.ps1 `
  -ExperimentId ma_positive_first_motion `
  -FirmwareCommit bfb60a7 `
  -Direction Positive `
  -PeakPwm 100
```

输出目录默认为 `experiments/generated/<ExperimentId>/`，包含：

- `command_schedule.pending.csv`：待执行命令计划；
- `experiment_manifest.json`：参数、安全检查和授权状态。

首次运动计划的软件临时上限为 200，低于固件 G1.2 的 840 硬上限。正转和反转必须分别生成、分别观察，首个计划不会在一次运行内直接换向。工具按不超过 400 ms 的间隔重复 PWM 命令，低于固件 500 ms 命令超时；结束使用受控 `STOP`，不生成需要复位恢复的 `ESTOP`。

生成后的清单固定为 `execution_state=not_authorized`，并明确记录生成过程不会打开串口；供电、极性/共地、MA 单通道、车轮悬空、紧急断电和人工授权初值均为 `false`。文件生成成功不代表允许执行。

## 电机动力执行门

真正执行前必须暂停并由操作者确认：

1. 12 V 电机动力来源、极性和可用电流符合当前单电机试验；
2. 控制板逻辑地与电机动力地连接正确；
3. 只允许 MA 运动，MB/MC/MD 保持 Coast；
4. 车轮悬空、周围无干涉，机器人被可靠固定；
5. 可立即切断电机动力，且操作者已了解 `ESTOP` 后必须复位；
6. 串口、ST-Link 和遥测仍正常；
7. 明确本次方向、PWM、持续时间和全部待发送命令。

上述条件未关闭前，不得把 `.pending.csv` 传给带 `-AllowNonStatusCommands` 的采集命令。

## 采集后的离线分析

对提交二生成的完整采集目录运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\analyze_g2_capture.ps1 `
  -CaptureDirectory .\captures\<capture-directory>
```

分析器先核对原始字节、完整行分区、遥测行数、命令行数和解析错误。串口采集允许在遥测帧中间开始或结束，因此非遥测首行和尾部残片作为边界事实保留，不再被误判为内容损坏；原始字节始终是权威证据。证据计数不闭合时拒绝继续。通过后生成：

- `g2_analysis_summary.json`：采样周期、时序门槛、PWM、电池、四路编码器计数率和错误最大值；
- `g2_telemetry_derived.csv`：相邻帧板端时间差、控制 tick 差、四路累计计数差和 counts/s。

分析结果仍保留 counts/s；轮端 122,880 counts/rev 已由三圈实测关闭，但 RPM 字段必须等待速度估计模块正式实现后再进入遥测，不能由离线工具伪装。P99 抖动来自 50 Hz 快照，只作为初步检查；累计最大抖动、WCET、漏周期和截止期计数仍可直接发现明显阻断问题。

## 已完成的动力首动检查点

2026-07-18 在 3S 电池约 11.7 V、车轮悬空、COM6/USART2 230400 8N1 条件下完成 MA 正向首动：

- `PWM 100` 两次均未观察到电机自主转动；第二次出现的编码器计数由操作者手动转轮产生，不能作为电机运动证据；
- `PWM 200` 两次均由板端实际施加后受控回到 0；操作者现场确认第二次试验中 MA 车轮连续向后转动；
- 被现场确认的 `PWM 200` 试验中，MA 编码器累计增加 13,006 counts，MB/MC/MD 均为 0；软件正向当前对应物理向后、编码器正计数；
- 电池范围为 11.692-11.727 V，无故障、急停、命令拒绝或 ADC 错误，结束状态为未使能且目标/实际 PWM 均为 0；
- 后续编码器自动二分把正向启动区间收敛到 `(150, 175]`，但不把悬空单电机结果直接冻结为最终死区补偿值。

2026-07-18 随后完成 MA 轮端整圈计数复测：45 秒纯 `STATUS` 采集包含 12.8 秒转前平台、三圈人工向前转动和 20.9 秒转后平台；MA 累计变化为 -371,783 counts，平均绝对值 123,927.7 counts/rev。相对 122,880 的偏差约 0.85%，相对 30,720 高约 4 倍，因此确认原厂 1024 PPR 需要按 AB 相四倍频和 1:30 减速比计算，轮端配置修正为 122,880 counts/rev。人工标记误差使本次结果只用于倍率确认，不替代未来自动化精密标定。

## 编码器自动死区判定

`new_g2_deadzone_probe_plan.ps1` 生成单一方向、单一 PWM 的受控计划，允许范围为 1-840，但仍默认 `not_authorized` 且不打开串口。`classify_g2_deadzone_capture.ps1` 对完成的采集离线判定：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\classify_g2_deadzone_capture.ps1 `
  -CaptureDirectory .\captures\<capture-directory> `
  -Direction Positive
```

默认只有 MA 沿期望符号的最大累计位移达到 1,000 counts、反向位移未越界、其他三个通道均未越界、最终目标/实际 PWM 与电机状态归零且全部通信/ADC 错误为 0，才接受为起转。该阈值用于排除几十至数百 counts 的抖动，不代表速度闭环的最低可控速度。

2026-07-18 在电池 11.701-11.718 V、车轮悬空条件下完成二分：

- 正向：`+150` 最大正向位移 136，判定未起转；`+175` 两次分别为 7,433 和 13,171，均可靠起转，区间为 `(150, 175]`；
- 反向：既有 `-200` 两次未起转；`-520/-360/-280/-240/-220` 分别产生 53,729/36,799/27,380/22,839/17,840 counts 的负向位移，`-220` 复验为 17,318，区间为 `(200, 220]`；
- 全部试验结束状态安全，其他三个编码器通道最大位移不超过 1 count，无故障、急停、命令拒绝或 ADC 错误。

这些区间只描述当前电池、温度、悬空和静摩擦条件。最终控制前馈必须在承重、多电压和重复启动条件下重新评估，并为可靠启动保留裕量。

## 悬空多档工作点扫描

`analyze_g2_operating_point.ps1` 对单一方向、单一幅值的完整采集进行离线工作点分析：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\analyze_g2_operating_point.ps1 `
  -CaptureDirectory .\captures\<capture-directory> `
  -Direction Positive `
  -PeakPwm 400
```

分析器只把目标 PWM 与实际 PWM 同时达到指定幅值、并排除最初 500 ms 收敛段后的连续窗口作为稳态窗口，输出带符号的平均 counts/s、由已验证的 122,880 counts/rev 离线换算的轮端平均 RPM、相邻遥测计数率中位数、PWM 爬升时间、1,000 counts 运动阈值延迟和带载电池电压。离线 RPM 只用于本次辨识数据汇总，不代表固件已经实现或验收轮速估计器。采集不完整、方向错误、其他编码器越界、最终未安全停机或任一通信/ADC 错误非零时，结果不会被接受。

2026-07-18 在约 11.7 V、车轮悬空、每档目标保持约 3 s 条件下完成一次双向扫描：

| 软件方向 | PWM | 稳态窗口 ms | 平均 counts/s | 离线轮端 RPM | 运动阈值延迟 ms | 最低电压 V |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 正向 | 240 | 2266 | 26,814 | 13.093 | 264 | 11.683 |
| 反向 | 240 | 2244 | 22,592 | 11.031 | 528 | 11.692 |
| 正向 | 400 | 2080 | 51,339 | 25.068 | 264 | 11.683 |
| 反向 | 400 | 2090 | 45,995 | 22.459 | 264 | 11.692 |
| 正向 | 600 | 1892 | 82,513 | 40.290 | 264 | 11.656 |
| 反向 | 600 | 1870 | 77,107 | 37.650 | 198 | 11.692 |
| 正向 | 840 | 1649 | 120,916 | 59.041 | 198 | 11.674 |
| 反向 | 840 | 1620 | 114,768 | 56.039 | 374 | 11.674 |

在这四个已起转工作点内，单次最小二乘结果为：

- 正向：`RPM ≈ 0.076598 × PWM - 5.4579`，`R² ≈ 0.99991`；
- 反向幅值：`RPM ≈ 0.075202 × |PWM| - 7.3103`，`R² ≈ 0.99979`；
- 同幅值下正向比反向快约 2.06-3.00 RPM，相对差异从 PWM 240 的约 17.1% 降至 PWM 840 的约 5.2%。

高 `R²` 只说明本次悬空、单次、已起转区间近似线性，不能外推到零点，也不能代替动态模型。线性拟合外推的零速交点与实测死区不一致是预期现象；死区继续使用 `(150,175]` 和 `(200,220]` 的直接试验边界。悬空重复性筛选结果见下节；承重、多电压、温升、阶跃/斜坡和零点穿越数据仍未完成，不能据当前数据比较一阶/二阶加纯延迟模型。

## 悬空多档重复性筛选

`summarize_g2_repeatability.ps1` 从清单读取各采集目录中的 `g2_operating_point_summary.json`，按方向和 PWM 分组计算样本标准差、CV、最大单次偏离、最低带载电压和双向均值单调性。门槛固定为：至少三次，CV 不超过 5% 才通过；5%-10% 增加两次复验；CV 超过 10%、单次采集未被接受或均值不单调时不得进入动态建模。

2026-07-18 保持板上固件 `ad0fd37`、约 11.7 V、MA 单通道和车轮悬空，完成第二轮顺序扫描、冷却后第三轮反序扫描，并对反向 240 补两次复验。26 次工作点采集均单次接受，结果如下：

| 软件方向 | PWM | 次数 | 平均轮端 RPM | CV % | 最大单次偏离 % | 最低电压 V | 筛选 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 正向 | 240 | 3 | 13.352 | 3.673 | 4.236 | 11.656 | 通过 |
| 正向 | 400 | 3 | 25.306 | 1.499 | 1.729 | 11.647 | 通过 |
| 正向 | 600 | 3 | 40.492 | 0.815 | 0.940 | 11.656 | 通过 |
| 正向 | 840 | 3 | 58.904 | 0.207 | 0.232 | 11.656 | 通过 |
| 反向 | 240 | 5 | 12.162 | 6.540 | 9.295 | 11.639 | 需复验 |
| 反向 | 400 | 3 | 23.160 | 3.417 | 3.705 | 11.674 | 通过 |
| 反向 | 600 | 3 | 38.059 | 1.349 | 1.515 | 11.647 | 通过 |
| 反向 | 840 | 3 | 56.221 | 1.144 | 1.271 | 11.656 | 通过 |

双向平均速度均随 PWM 单调增加，但反向 240 在规定的两次补测后仍为 `CV=6.540%`，因此总筛选保持未接受，禁止据此放行动态模型。该现象位于反向悬空死区上沿附近，后续应优先检查机械静摩擦、温度/停放姿态和试验顺序敏感性，而不是继续无上限追加相同工况。

重复性测试前曾出现两次纯 `STATUS` 收到 0 字节，通信恢复后又读到最低 4.139 V；在任何运动命令前，后续两次独立纯 `STATUS` 已恢复为 11.674-11.727 V 并通过全部停机/错误门槛。串口证据只能确认动力采样节点当时掉到近似逻辑供电或回灌电平，不能区分插头、开关、保险、共地或其他动力链瞬断。最终纯 `STATUS` 为 11.647-11.683 V、目标/实际 PWM 0、`motor_state=0`、故障/急停/全部错误为 0。

### 反向 240 固定初始轮位诊断

2026-07-19 将 MA 车轮每次从同一方向转回相同实体标记后，重新执行三次反向 240。三次轮端速度为 12.522/12.939/13.078 RPM，均值 12.847 RPM、样本标准差 0.289 RPM、`CV=2.253%`、最大单次偏离 2.525%，固定轮位分组通过 5% 门槛。三次最低带载电压均为 11.639 V，方向、其他通道、最终停机和全部错误门槛均通过；结束纯 `STATUS` 为 11.639-11.656 V。

该结果支持“初始机械相位或回差是此前低速离散的重要变量”，但单一轮位不能证明所有角度均稳定，也不能替代原 26 次全局筛选。下一步应在多个预先定义轮位上重复相同工况，保持靠近方向、静置时间和电压一致；只有各轮位内和轮位间差异均受控后，才允许关闭反向 240 阻断项。

### 反向 240 多轮位对照

2026-07-19 继续在相对原标记约 120° 和 240° 的轮位执行对照。操作者随后确认 120° 前五次的实体标记可能未贴紧，因此这些原始采集继续保留但不进入正式判定；重新固定标记后的后三次 12.076/12.663/12.753 RPM 才作为有效 120° 组，均值 12.497 RPM、`CV=2.942%`、最大偏离 3.372%。0° 三次为 12.522/12.939/13.078 RPM，均值 12.847 RPM、`CV=2.253%`；240° 三次为 12.047/12.412/12.766 RPM，均值 12.408 RPM、`CV=2.898%`。

按三个轮位各三次有效样本组成的 9 次主对照，整体均值约 12.584 RPM、`CV=2.835%`、最大单次偏离 4.267%；三个轮位均值的极差约为均值的 3.481%。各轮位组和跨轮位统计均通过 5% CV 与 10% 单次偏离门槛，反向 240 在已选 0°/120°/240° 悬空轮位下通过重复性筛选。全部有效与被排除采集仍保留原始证据且均单次安全接受；最低带载电压 11.603 V，结束纯 `STATUS` 为 11.603-11.647 V。

修正后的有效样本不支持“机械角位置导致超过门槛的显著离散”结论；此前异常主要受不可靠实体标记这一试验条件污染。该结论只关闭已选三个悬空轮位的反向 240 重复性门，承重、多电压、温升、阶跃/斜坡、零点穿越和 Brake/Coast 仍需继续执行，不能直接进入动态建模。

## 板内受控动态阶跃

`new_g2_dynamic_step_plan.ps1` 为单一方向、单一幅值生成一个 14 s 独立计划。只允许 240/400/600/840 PWM，固定包含纯 `STATUS`、`CAPTURE START/STOP/STATUS/EXPORT`、`ARM`、四次 300 ms 间隔的同幅值 `PWM` 和受控 `STOP`；不在同一次计划内反转，也不生成 `ESTOP`。固件仍以 `1 count/ms` 斜坡限制实际 PWM，因此这里辨识的是“安全斜坡后的实际输入响应”，不是把目标命令误称为理想瞬时阶跃。

首个建议批次使用中档 `±400`、每个方向三次。批次生成器交替正反方向先后次序，降低固定试验顺序偏差：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\new_g2_dynamic_step_batch.ps1 `
  -BatchId g2_dynamic_midrange_pilot `
  -FirmwareCommit cc1b43a `
  -PeakPwms 400 `
  -Repetitions 3 `
  -SequenceStart 3000
```

批次和每个子计划都保持 `execution_state=not_authorized`。现场必须一次只执行一个子计划；每次动作前重新做独立纯 `STATUS`，确认动力电压至少 10.5 V、MA 停机、运行就绪且全部错误为 0，并由操作者明确授权。计划间至少冷却 30 s；任一次采集被拒绝，就停止整个批次，不继续补发后续运动。

现场授权后，单个子计划使用对应的 `command_schedule.pending.csv`：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\capture_serial.ps1 `
  -Port COM6 `
  -DurationSeconds 14 `
  -FirmwareCommit cc1b43a `
  -CommandSchedulePath .\experiments\generated\<批次>\<子计划>\command_schedule.pending.csv `
  -AllowNonStatusCommands
```

每次采集后立即运行严格分析器：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\analyze_g2_dynamic_step.ps1 `
  -CaptureDirectory .\captures\<采集目录> `
  -PlanDirectory .\experiments\generated\<批次>\<子计划>
```

分析器先复用 schema 2 高速门，再核对板上固件提交、13 条计划/实发命令和不超过 100 ms 的调度误差；随后检查目标幅值、实际 PWM 符号和峰值、升降斜坡、至少 50 个基线/峰值样本、期望方向至少 1,000 counts、错误方向/其他通道不超过 1,000 counts、最低动力电压、最终停机和全部错误计数。只有所有门同时通过，单次 `accepted` 才为 true。

六次单项分析完成后，用 PowerShell 数组把采集目录绑定到批次：

```powershell
$captures = @(
  '.\captures\<正向第一轮>',
  '.\captures\<反向第一轮>',
  '.\captures\<反向第二轮>',
  '.\captures\<正向第二轮>',
  '.\captures\<正向第三轮>',
  '.\captures\<反向第三轮>'
)
.\tools\summarize_g2_dynamic_step_batch.ps1 `
  -BatchDirectory .\experiments\generated\<批次> `
  -CaptureDirectories $captures
```

批次工具分别输出采集证据是否完整和重复性筛选，不把二者混为一谈。峰值窗口 RPM 的样本 CV 不超过 5% 为通过，5%-10% 要求补两次，超过 10% 阻断；无论结果如何都固定 `model_ready=false`。

连续批次不能只依赖单次最低运动阈值。同方向、同幅值已有至少两次接受结果后，每次新结果必须在进入下一计划前运行 `check_g2_dynamic_cross_repeat.ps1`：以既有结果中位数为基准，峰值窗口 RPM 和总位移必须保持在 50%-150%，运动阈值延迟不得超过 2 倍。该门只负责及时发现数量级变化，不能代替批次结束后的 CV 筛选；未评估或未通过时均不得继续下一动作。

2026-07-19 使用固件 `cc1b43a` 完成悬空 `±400` 各三次中档试点。六次现场观察均正常，单次 schema 2 严格分析全部接受，升降斜坡均为 399 ms，其他三个编码器均为 0，最低动力电压 11.594 V。正向峰值窗口为 18.922/20.701/20.852 RPM，均值 20.158 RPM、`CV=5.327%`；反向为 17.335/18.673/17.793 RPM，均值 17.934 RPM、`CV=3.791%`。因此反向通过，正向进入补两次区间；正向均值比反向高 2.225 RPM，约占双向均值的 11.68%。

随后按同一计划补两次正向 400，峰值窗口为 19.457/20.827 RPM，两次现场观察和单次严格门均通过。合并正向五次后均值 20.152 RPM、样本标准差 0.901 RPM、`CV=4.469%`、最大单次偏离 6.104%，通过 5% 门槛；反向三次保持 `CV=3.791%`。八次合并结果为 `capture_evidence_accepted=true`、`repeatability_accepted=true`、`model_ready=false`，正向均值比反向高 2.218 RPM，约占双向均值的 11.65%。补测最低动力电压 11.576 V，动力断开后纯 `STATUS` 回到 3.758-3.820 V。

本批次关闭中档 `±400` 悬空动态重复性门，但仍需扩展其他幅值、斜坡、零点穿越、Brake/Coast、承重、多电压和温升工况并划分独立验证集，才允许比较一阶/二阶加纯延迟模型。

### 其余幅值连续试验的中止记录

2026-07-19 计划连续执行 `±600`、`±240`、`±840` 各三次。`±600` 六次全部接受：正向三次均值 35.061 RPM、`CV=3.402%`，反向三次均值 31.169 RPM、`CV=2.175%`，双向均通过。

`+240` 三次均值 11.412 RPM、`CV=1.766%`，通过。`-240` 前两次为 8.825/9.180 RPM、16,338/17,112 counts，第三次却只有 1.084 RPM、3,399 counts，运动阈值延迟增至 906 ms。该次仍超过原单次 1,000 counts 最低门，因此旧自动流程未及时停止；保留异常样本并增加一次独立补测后，补测恢复到 9.473 RPM、17,407 counts、249 ms，但四次合并峰值 `CV=56.669%`、位移 `CV=50.071%`、延迟 `CV=82.684%`，反向 240 明确阻断。

在外部停止命令生效前，流程已完整完成一次 `+840`，该次得到 51.369 RPM、95,614 counts 并通过全部单次严格门；下一次 `-840` 尚未发送任何命令。随后精确终止主机进程并下发 `ESTOP`，板端确认 PWM 0 和 Emergency Coast；操作者复位只用于执行上述一次 `-240` 补测。840 批次保持暂停，不得把单次正向结果视为重复性结论。

### 反向 240 全新统计组

操作者明确要求此前连续流程和复位后补测的旧 `-240` 不再纳入新的正式统计。执行时采用“统计排除、证据保留”：旧原始目录、异常数值和 ESTOP 处置记录不得删除，也不得据此改写历史结论。

2026-07-19 使用固件 `cc1b43a` 重新执行三个独立反向 240 计划。每次动作前纯 `STATUS` 均确认约 11.6 V、停机、运行就绪和零错误；三次分别得到 14,307/15,486/16,634 counts、8.120/8.754/9.126 RPM 和 380/276/252 ms 延迟。每次升降斜坡均为 239 ms，其他三个编码器无变化，单次严格门全部接受；第三次相对前两次的跨重复在线门通过。

只汇总这三次新结果时，峰值 RPM 均值为 8.667、样本标准差 0.509 RPM、`CV=5.869%`；位移均值为 15,475.7 counts、`CV=7.518%`；延迟均值为 302.7 ms、`CV=22.480%`。按既定峰值速度 `CV<=5%` 通过、`5%-10%` 补测规则，三次结果为 `retest`，因此保留三次并补到五次。

第四、第五次分别得到 13,157/16,162 counts、7.405/8.722 RPM 和 385/244 ms 延迟，单次严格门和跨重复门均通过。五次整体峰值 RPM 均值为 8.425、样本标准差 0.675 RPM、`CV=8.009%`、最大单次偏离 12.112%；位移 `CV=9.348%`，延迟 `CV=22.636%`。工具筛选仍为 `retest`，但允许的五次上限已用完，工程门保持未关闭，不再追加同条件重复。

结束上电纯 `STATUS` 为 11.576-11.603 V，断开动力后为 3.891-3.926 V，均停机且错误为 0。下一步在不接动力的情况下设计受控机械条件诊断：固定并记录初始轮位、单向靠近方式、静置时间、温度和电压；评审后再生成新的板测计划。840 和模型拟合继续暂停。

### 反向 240 启动与稳定速度独立验证

原动态摘要的 `peak_window_wheel_rpm` 使用从实际 PWM 首次达到 -240 到目标 STOP 的整个平台，现有五次平台仅约 705-719 ms，起转等待会直接拉低整窗均值。离线固定计算平台结束前 200/300 ms 后，五次速度均值为 9.328/9.340 RPM、`CV=3.674%/4.393%`、最大单次偏离 5.224%/6.998%；首个负向编码器增量延迟为 71-95 ms、`CV=11.566%`，1,000 counts 延迟为 244-385 ms、`CV=22.636%`。探索性分类为 `stable_speed_variable_startup`，但不得用同一批数据把旧短窗口结论改写成通过。

独立验证固定三次，不允许追加第四次。每次从 0° 实体标记按相同物理方向靠近，不允许越过后反向回调；至少冷却 60 s，纯 `STATUS` 动力范围必须为 11.500-11.750 V。按操作者决定，本批次不测量或记录温度，温度也不参与接受门；该简化意味着结果不能外推为跨温度稳定。新计划在 850 ms 下发 -240，保持至 2300 ms，预计实际满 PWM 平台约 1.211 s，再受控 STOP；高速缓冲仍在 2200 样本预算内。

固定判据为：三次单次严格门和跨重复门全部通过；末端 200 ms 与 300 ms 的 RPM `CV` 必须同时不超过 5%，两个窗口最大单次偏离必须同时不超过 10%。启动延迟单独按 10% CV 报告，但不与稳定速度混成一个指标。只有独立批次通过，才允许把 240 标记为“稳态速度可重复、启动过程非线性”的验证点；它仍不自动进入普通线性模型拟合，`model_ready` 保持 false。

每次采集后仍先运行统一单次严格分析器。该入口会按 `g2_low_speed_steady_validation` 类型校验 14 条计划/实发命令、完整高速门和既有动态安全门，并额外要求实际满 -240 平台至少 1200 ms；单次摘要中的 `low_speed_diagnostic` 直接给出固定末端 200/300 ms 速度，避免批次结束后才发现窗口不完整：

```powershell
& .\tools\analyze_g2_dynamic_step.ps1 `
  -CaptureDirectory .\captures\<本次采集目录> `
  -PlanDirectory .\experiments\generated\<独立验证批次>\<本次子计划>
```

三次单次都接受后，再把三个采集目录一次性传给 `analyze_g2_low_speed_diagnostic.ps1`，并明确使用 `-EvidenceRole independent_validation -RequiredRepeatCount 3`。任何单次拒绝都立即中止后续动作，不得用最终批次 CV 掩盖单次证据失败。

离线复跑现有五次：

```powershell
& .\tools\analyze_g2_low_speed_diagnostic.ps1 `
  -CaptureDirectories @(
    '.\captures\20260719-152304066_COM6',
    '.\captures\20260719-152525386_COM6',
    '.\captures\20260719-152818633_COM6',
    '.\captures\20260719-154240198_COM6',
    '.\captures\20260719-154450829_COM6'
  ) `
  -EvidenceRole exploratory `
  -RequiredRepeatCount 5 `
  -OutputDirectory '.\experiments\generated\g2_low_speed_exploratory_20260719'
```

## 动力接入后的执行顺序

1. `[已完成]` 正向低 PWM 首动，确认 MA 通道、编码器符号、STOP 和电池压降；
2. `[已完成]` 单独执行负向低 PWM 首动，确认方向与编码器符号对称；
3. `[已完成]` 测量输出轴整圈计数，关闭编码器倍率冲突；
4. `[已完成]` 用完整 1 kHz 样本通过无动力和带动力 TIM7 零丢周期、P99/最大抖动、唤醒延迟、WCET 及通信严格门；
5. `[600 已通过，反向 240 独立验证待执行，840 暂停]` `±400` 和 `±600` 动态重复性已通过，`+240` 通过；全新五次原短窗口 `CV=8.009%`，探索性末端 200/300 ms 为 `CV=3.674%/4.393%`，现按固定双窗口执行最多三次独立验证；`+840` 仅有一次接受结果，其余 840 暂停；
6. `[悬空工作点已执行]` 每个新增工况仍至少重复三次，并保留电池状态、温升和机械配置；
7. 比较一阶加纯延迟、二阶加纯延迟模型；
8. 增加脉冲周期测量后比较 M、T 与 M/T 测速，再决定 RPM 实现。
