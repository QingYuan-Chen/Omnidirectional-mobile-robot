# 下位机固件任务计划

## 状态定义

- `[基线]`：软件实现已经存在，并通过构建或静态检查。
- `[待板测]`：软件实现已经存在，但仍需硬件验证。
- `[待实现]`：尚未实现。
- `[待确认]`：缺少硬件身份或额定参数证据，确认前不得代入参考值。
- `[待决策]`：候选和证据尚未完成统一评审，不得写成冻结方案。

## M0：仓库、规划与阻塞缺陷

- `[基线]` 初始化 Git 仓库、排除生成物与参考资料，并向 `origin/main` 推送基线。
- `[基线]` 记录 FreeRTOS 任务框架、QMI8658A 同步读取、静止标定和 6 状态 ESKF 设计。
- `[基线]` 修复 UART 接收完成回调检测到错误后无法可靠恢复接收的问题。
- `[基线]` 使用 Debug/Release 构建、严格告警和静态分析验证 UART 修复。

## M1：IMU 数据质量与可恢复故障

- `[基线]` 输出原始帧、传感器时间戳、主机时刻、序号、错误计数、丢样/重复计数和处理后有效性标志。
- `[待板测，随 M2 单电机调试执行]` 验证 QMI8658A 的 3 ms 固定轮询、同步采样锁、轴映射、量程、ODR 和时间戳回绕。当前 H60 仅把 QMI8658A INT1 接到 PD7，而普通 DRDY 固定从 INT2 输出，因此本版不再把 PD7/INT1 当作 DRDY。
- `[待板测，随 M2 单电机调试执行]` 增加独立的滤波加速度/角速度输出，供控制和遥测使用；不滤波 ESKF 输入。
- `[待板测，随 M2 单电机调试执行]` 增加可配置的突变拒绝与诊断计数，最终阈值由板上日志确定。
- `[待板测，随 M2 单电机调试执行]` 在 `AppImuOutput` 增加 `sample_age_ms`。
- `[基线]` 将阻塞式固定延迟改为非阻塞 `20/50/100/200/500 ms` 退避。
- `[基线]` 分离任务存活、瞬时传感器降级、持久传感器故障和估计器故障。
- `[基线]` 恢复处理后有效状态前，要求连续稳定的有效样本。
- `[基线]` 删除 `UINT32_MAX` 丢样哨兵，保留真实的饱和计数。
- `[基线]` 统一启动、安全、通信和控制执行端的实时 IMU 运动判据；运行就绪必须同时满足关键任务心跳完整和 ESKF 收敛。
- `[基线]` `ARM/PWM/STOP` 在运行许可关闭时不入队、不提交序号；`STATUS/ESTOP` 始终独立可用，许可代际保证故障前已排队命令恢复后仍失效。
- `[待板测，随 M2 单电机调试执行]` 验证干扰恢复、数据陈旧、拔插、任务栈余量和 CPU 占用。

## M2：单电机闭环控制

- `[基线]` 建立 `m2_control_architecture_gate.md`，撤销 100 Hz 和 PI + 前馈的冻结结论，定义分层时钟、辨识和统一评测门槛。
- `[已完成]` 完成 G0：确认 XTark MC520P30_12V、12 V、1:30、1024 PPR 磁编码器及原厂额定参数；确认 LB350E 3S 5000 mAh 电池、11.1/12.6 V、8 A 持续/16 A 瞬时放电、230 g、2.2 kg 整车空载质量、约 10 kg 最大额外载荷和 77 mm 轮径；MA 整圈实测关闭 122880 counts/rev 与方向关系。项目不再把 BMS 精确动作参数、分级承重、厂家温升/绝缘数据或四轮同时启动测试作为 G0 或后续路线的必做项。
- `[基线]` 完成 G1.0：审查控制资源，确定 TIM6 保留为 HAL 时基、TIM7 作为 1 kHz 候选控制节拍，并冻结 ISR、任务通知、编码器快照、安全链、UART 和 ADC 的实施边界。
- `[部分板测完成]` 完成 G1.1 软件部分：TIM7 确定性控制节拍、带序号编码器快照、累计漏周期检测和时基诊断已实现；无动力及多次带动力 1 kHz 高速采集均达到零索引/tick 断点、零漏周期/超期以及既定 P99 抖动、最大抖动和 WCET 门槛。五任务栈水位、最小剩余堆和延期的 M1.3 联合验收仍未关闭。
- `[部分板测完成]` 完成 G1.2 软件部分：只允许 MA 输出的安全开环状态机已完成方向、限幅、1 count/ms 斜坡、ARM/STOP、ESTOP 及停车后复位链的多轮实测；500 ms 命令超时、Brake/Coast 细分行为和故障前旧队列命令在恢复后不补执行仍需集中故障注入验收。
- `[部分板测完成]` 完成 G1.3 软件部分：临时 ASCII 命令、非阻塞发送和故障诊断已通过历史 COM6/USART2 多轮实采。2026-07-21 已在软件和两份 CubeMX 配置中恢复 USART2 为树莓派正式通信口，并把电脑调试链迁移到无线 DAPLink 的 USART1/COM10；提交 `7aaaeb4` 已刷写并通过阶段 3 分类型遥测板测。G1.3 其余拥塞/错误注入债务仍按阶段 5-6 计划保留。
- `[已完成，历史链路]` 完成 M2 板测提交一：`commTask` 的命令接收、异步发送、端口错误统计和遥测统一使用集中式调试串口配置；当时选择 `BSP_UART_ROS/USART2`，不在 USART2 同时运行 ASCII 与 X-Protocol。2026-07-18 已在当时记录的 ST-Link 单独供电、电机无动力且车轮悬空条件下，通过板载 USB-COM/CH9102 的 COM6 完成 230400/8N1 遥测接收、`STATUS` 收发和三次串口关闭/重开验收。该历史证据保持有效，但当前阶段 3 调试链已经迁往无线 DAPLink/USART1，USART2 恢复为树莓派口。
- `[已完成]` M2 板测提交二：增加 Windows 串口采集工具与主机测试，明确选择 COM 口并保存原始日志、解析 CSV、命令时间线和测试元数据。2026-07-18 已用 Windows PowerShell 5.1 对 COM6 完成 5 s 安全实采，原始字节、228 行遥测、1 条 `STATUS` 时间线和元数据相互一致，解析及板端错误计数均为 0。
- `[当前执行顺序]` 阶段 3、4 已关闭，当前路线移动到阶段 5但尚未开始阶段 5；随后依次关闭阶段 5、6，再进入阶段 7。G2/G3 所有尚需连接主控板的验证统一归入阶段 7，不在阶段 5-6 之间零散穿插；已经完成的纯算法、分析器和计划生成器只作为阶段 7 的上板前准备。
- `[阶段 3 已完成]` schema 1 的 `STAT/IMUQ/RES/EVENT` 分类型低频遥测、Windows 分型保存/兼容视图、75% 最坏带宽门和 USART1/DAPLink 链均已关闭。权威证据 `captures/20260721-145813693_COM10/` 为总体系统上电、提交 `7aaaeb4`、COM10 230400/8N1、10 s，仅发送 1 条 `STATUS`：31,533 B，174/87/18/4 条 `STAT/IMUQ/RES/EVENT`，四类解析错误、非遥测行和尾残片均为 0；前三类频率为 19.871/9.935/1.998 Hz，四类板端时间单调，EVENT 相邻至少 100 ms。电池 11.479-11.541 V，目标/实际 PWM 始终 0，最终 ready=1、inhibit/fault/motor_state/ESTOP=0；通信、格式、入队、控制时序和 ADC 门均为 0。历史失败/诊断目录 `20260721-143006157_COM10`、`20260721-145501089_COM10`、`20260721-145602046_COM10`、`20260721-145717478_COM10` 全部保留。末行 `IMUQ dropped_sample_count=1128` 仅证明诊断如实可观测，真实 ODR/丢样仍是阶段 4/5 债务，不反写为阶段 3 通信失败；五任务栈和最小堆也只作为阶段 5 观测，不关闭阶段 5。
- `[历史板测证据，阶段 7 冻结]` M2 板测提交三：已完成 `±240/±400/±600/±840` 三轮悬空扫描及反向 240 的 0°/120°/240° 多轮位对照。排除操作者确认标记未贴紧的 120° 前五次后，三个有效轮位组内 CV 为 2.253%/2.942%/2.898%，9 次整体 `CV=2.835%`、最大单次偏离 4.267%，悬空重复性门通过。
- `[阶段 4 已完成；当前路线到阶段 5但尚未开始]` 阶段 4 的完成边界仅为板内 IMU 新接受样本高速采集、停车后完整导出和主机严格分析闭合。权威证据 `captures/20260721-160532766_COM10/` 使用固件 `3c6a714`、仓库 `62f1799`、COM10 230400、30 s、无 `ARM/PWM`；metadata schema 4 的 raw-first/offline-parse 记录在线 30,003 ms、离线解析 3,027 ms、211,123 B raw、1,152 条/27,648 B timing sidecar、bounded drain 0。共得到 1,223 条 IC、5 条 ICAP，STARTED=0，STOPPED/STATUS/BEGIN/END 均为 1,223/1,700且记录器三计数为 0；严格分析 `accepted=true`、13/13，索引、接受序号、STATUS0 和源 drop 门全部通过。sensor timestamp 每步为 1、总 1,222 steps=5,450.491 ms，host tick 5,520 ms，误差 69.509 ms<109.010 ms，host delta 3-6 ms；IMUQ 与板端错误最大值均为 0。结束状态 11.470 V、PWM/`motor_state`=0、ready=1且无 inhibit/fault/ESTOP。温度 raw 8,148-8,164（约 31.828-31.891 °C）关闭“温度恒 0”疑点。开头约 20 ms 的无线桥旧缓存边界仍有 1 个 STAT 解析错误和 2 个非协议残段，发生在 CAPTURE START 实际 501 ms 前，必须保留且不得写成全流解析 0；它不进入本次 IMU 严格链。前两份主机失败证据继续保留。静置模长约 8,738.873 counts（约 1.067 g）仅作观测；无已知姿态基准，轴向/量程转阶段 5，24 位短窗未发生回绕也转后续长时测试，不随阶段 4 误关。
- `[阶段 7：G2/G3 统一上板阶段，待阶段 5-6 关闭后恢复]` 已有证据保持不变：悬空 `±400`、`±600`、`+240` 及当前连接配置的 `-240` 稳态重复性通过，旧连接低响应完整保留。上板前软件准备已完成：G3 纯算法、互斥 `SC/SCAP` 诊断、TIM2 CC1 周期快照、Windows 分析链，以及 `±840` 专用严格工具链均已通过离线验证。阶段 7 统一在总体系统上电状态执行全部剩余 G2/G3 板测：先保持 MA 零输出且不发送 `ARM/PWM`，完成人工手转的真实 CC1 事件数、方向、错误计数和 IRQ/控制时序门；再经独立纯 `STATUS` 安全门放行运动测试，完成 M/T 速度段比较及当前连接配置的 `±840` 正反各三次新批次；最后形成完整辨识数据和正式模型独立验证。阶段 7 结束前始终保持 `model_ready=false`。
- `[已决策]` 完成 G4：当前 M2 固定现有 H60/AT8236 硬件，暂不引出或调理 `ISEN`，不更换电机驱动器，也不增加 MCU 可见的真实电流反馈；因此本版不实现电流内环、不使用估算电流冒充反馈。若未来启动硬件改版，必须重新打开 G4 并单独评审模拟前端、同步采样、保护和标定。
- `[待实现]` 完成 G5：在统一接口下实现 LADRC 基准、PI + 前馈、PI + DOB 和 host 仿真/测试。
- `[待板测]` 完成 G6：使用同一工况对比跟踪、抗扰、噪声、饱和恢复、温升、执行时间和鲁棒性。
- `[待决策]` 完成 G7：形成控制器、PWM、速度环和底盘环频率的正式决策记录。
- `[部分板测完成]` MA 软件正向已实测对应物理向后和编码器正计数，软件反向对应物理向前和编码器负计数；最终车辆坐标系符号及是否需要在 BSP/配置层统一留到轮序/车体系确认，不用控制器参数补偿接线符号。
- `[已完成]` MA 车轮三整圈完整起止平台累计 -371783 counts，平均绝对值 123927.7 counts/rev；确认 1024 PPR 需按 AB 相四倍频计算，轮端采用 122880 counts/wheel-rev 并排除 30720。
- `[待实现]` 根据实测轮端计数将编码器增量换算为轮速 RPM。
- `[阶段 7 上板前准备已完成]` 已实现 10 ms 可配置滑动 M 法、真实边沿聚合 T 法、带双阈值滞回的 M/T 候选，以及互斥 `G3_SPEED` schema 和受控 TIM2 CC1 IRQ。真实事件数、方向、错误计数、控制时序和同运行速度比较全部留在统一阶段 7 执行；阶段 5-6 关闭前不执行 G2/G3 板测。当前阈值均是可配置候选，不用未经验证的普通低通掩盖量化问题，也不得用 1 ms 计数插值冒充 T 法。
- `[待实现]` 根据 G7 结论在 `controlTask` 接入最终单电机控制链路。
- `[待板测]` G1.2 已实现 500 ms 命令超时、不可被新 PWM/STOP 绕过的受控停车、停稳后 Brake 和重新 ARM 门槛。
- `[轮速纯算法已覆盖，其余待实现]` 边沿回绕、换向清空、聚合溢出、T 法陈旧、M 窗口和 M/T 滞回已有 host 单元测试；全部候选控制器、限幅/复位和后续诊断集成测试仍待实现。
- `[待板测]` 根据 G6/G7 验证最终控制器，并完成延期的 M1.3 板测。

## M3：人机交互

- `[基线]` 仅在初始化、标定和滤波器就绪后发出 0.5 s 启动提示音。
- `[待实现]` 根据原理图确认 PE1 按键极性和上下拉，并增加消抖。
- `[待实现]` 实现短按启停和长按急停。
- `[待实现]` 实现启动、就绪、运行和故障 LED 状态机。
- `[待实现]` 实现非阻塞蜂鸣器 pattern，并将启动提示音迁入统一机制。
- `[待实现]` 扩展可配置调试串口监控：栈、堆、电池、IMU 和最终选定控制器状态；当前人类调试链固定使用无线 DAPLink/USART1，M5 在独立 USART2 上实现树莓派 X-Protocol，不得交换或混用两条链路。

## 后续里程碑

- `M4`：四电机闭环、麦轮运动学、轮序/符号约定和速度回传。
- `M5`：兼容原厂实现的 X-Protocol 与 ROS2 联调。
- `M6`：IWDG、UART DMA + IDLE、电池保护和生产级 CPU/栈验证。

## 已确认事实与未冻结候选

- MCU 当前运行在 168 MHz；不通过调整 SYSCLK 改变控制周期。
- PWM 当前约 20 kHz；G1.1 已把控制任务切换为 TIM7 候选 1 kHz 硬件节拍。两者仍是实验基线，不是最终冻结值。
- PWM 计数范围当前为 -4200 至 +4200；最终调制方式和限幅随 G7 决策。
- 电机已确认为 MC520P30_12V：12 V、1:30、1024 PPR 磁编码器版本。
- 编码器轮端分辨率已由 MA 三圈实测确认采用 122880 counts/wheel-rev；人工标记误差约 0.85%，该结果关闭倍率歧义，不替代未来自动化精密标定。
- 首个闭环验证对象：MA 电机。
- 当前硬件没有 MCU 可用的电机电流反馈，禁止实现伪电流环。
- 速度环以 1 kHz、机体环以 100-200 Hz 作为首轮实验候选；最终频率由 G7 决策。
- LADRC 是基准控制器，PI + 前馈和 PI + DOB 是对比候选；基准不等于最终方案，最终选型仍由 G6/G7 关闭。
- M0-M3 不包含麦轮运动学和二进制 ROS 协议。
